## ベストプラクティス
- 先例の意図も考慮しつつ、設計・言語・フレームワークのベストプラクティスに則ること

## 重要事項
- QAと言ったら x-QA/ に新規資料作成して記載。接頭辞は2桁連番 **_{タイトル}.md
- az が最初につくチャットに関しては「## 「az {パス}」と指示された場合」を参照し適切に解説ファイル作成

- docsと言ったら x-Docs/ に新規資料作成して記載。接頭辞は2桁連番 **_{タイトル}.md
- hisと言ったら その話を実行する前段として x-history/ に新規資料作成して記載
- チャット末尾 otは ファイル変更等はせず、ユーザに回答するのみ
- チャット末尾 spは 最速でシンプルな解答をする。調査しないと回答できない場合は推測で答えず「調査必要」と回答する。
- hoは handoffで引き継ぎファイルを作成すること x-handoff.md
- _old はどの階層においても指示がない限り観覧不要

- AGENTS.local.md も確認すること

## その他

Git 操作について、エージェントは以下のコマンドを直接またはスクリプトなどを介して実行してはならない

- `git add`
- `git commit`
- `git push`

## 観点箇所へのコメント追記
- 学習テーマとなる見るべき場所には、以下のように [■観点:{観点見出し}] をつける

  ```
  <!--
    [■観点:#workspaceRef] - #workspaceRef はテンプレート内で子コンポーネントを直接参照する #ref。
  -->
  <app-sample-03-workspace-panel #workspaceRef />
  ```

- 一般的な言語で、言い換えれる場合、伝わりやすい場合がある

```
// [■観点:viewChild] 簡単にいうと、子を直接操作するため子のインスタンスを取得しているだけです。
readonly workspace = viewChild.required(WorkspacePanel);
```

## az {パス}」と指示された場合

「解説 {パス}」と指示された場合、{パス}.code.mdを作成し、資料には
4本パッククォートで囲った、そのソースコードを全文コピーする。
さらに、特に行の指定がない場合、全行を対象に「末尾解説コメント」を追加する
- ある程度コメントのインデントが揃える（途中からインデント幅が変わっても良い）
- コメントにコメントは不要
- 閉じタグにコメント不要
- 資料の最初はコードブロックとし、解説はコードブロックの後に記載する
- 解説は 各観点 等を、ユーザのレベルに合わせ臨機応変な粒度で記載する
- コメントに敬語は不要
例: 
````
export class Foo implements OnInit {     // ...解説
  private readonly api = inject(BarApi); // ...解説
  baz() {            //  ...解説 ⇦ インデント幅は臨機応変に変えていい
    console.log(123) //  ...解説
  }
...
````
例2（良い例悪い例）:
````
  // 悪い例
  status: BarStatus;      // 状態の項目または返却データの項目を定義します。
  stateChangedAt: number; // 状態の項目または返却データの項目を定義します。⇦ 意味の無いコメント
  // 良い例
  status: BarStatus       // 状態の項目または返却データの項目を定義。Barのステータス
  stateChangedAt: number; // BarStatusの変更日時
````
