# 01-01 画面初期化とビュー構築

`/projects/:projectId/tasks` にルーティングされてから、子コンポーネントが立ち上がるまで。

`ExperimentsComponent` のコンストラクタは、購読の登録と Action の発行だけを行う。
データの取得は最後の `getExperiments()` を Effects が受け取ってから始まるため、
テンプレートは最初 `experiments$` が `null` の状態で描画される。

## 図1: プロバイダ登録からコンストラクタ完了まで

```mermaid
sequenceDiagram
    autonumber
    participant Router
    participant EC as ExperimentsComponent
    participant Base as BaseEntityPageComponent
    participant Store as NgRx Store
    participant Refresh as RefreshService

    Router->>EC: loadComponent()
    Note over Router,Store: route providers で provideState(EXPERIMENTS_STORE_KEY)<br/>と provideEffects([...]) が先に登録される

    EC->>Base: super()
    Base->>Store: dispatch getAllSystemProjects
    Base->>Base: effect(() => shouldOpenDetails = tableMode() !== 'table')
    Base->>Refresh: refresh.tick を購読
    Refresh-->>Base: (以後) tick → refreshList(auto)
    Base->>Base: setupBreadcrumbsOptions()

    EC->>EC: setSplitSizeAction / addTag を差し替え
    EC->>Store: dispatch initSearch('Search for tasks')
    EC->>Store: dispatch getProjectUsers({projectId, entity: Task})
    EC->>Store: dispatch setTableCols({cols: INITIAL_EXPERIMENT_TABLE_COLS})

    EC->>Store: select(selectRouterParams) を購読
    EC->>Store: combineLatest([routerProjectId, queryParams, customColumns]) を購読
    Note right of EC: 初回の値が届いた時点で URL 復元処理が走る<br/>（詳細は 02-02）

    EC->>EC: createFooterItemsRunner()
    EC->>EC: selectExperimentFromUrl()
    EC->>Store: dispatch getParents({searchValue: null})
    EC->>Store: effect: projectId() があれば getTags / getProjectTypes
```

## 図2: テンプレート描画で子に流れる input

```mermaid
sequenceDiagram
    autonumber
    participant EC as ExperimentsComponent
    participant Header as sm-experiment-header
    participant Split as as-split
    participant ExpTable as sm-experiments-table
    participant SmTable as sm-table
    participant Outlet as router-outlet
    participant Footer as sm-entity-footer

    EC->>Header: [tableCols] [isArchived] [metricVariants] [hyperParams]<br/>[tableFilters] [tableMode] [addButtonTemplate]
    EC->>Split: [size]=100-splitSize() / splitSize()
    Split->>ExpTable: 左ペインに配置
    EC->>ExpTable: [experiments] [tableCols] [colsOrder] [tableFilters]<br/>[checkedExperiments] [selectedExperiment]=highlited()<br/>[contextMenuTemplate]=contextMenuExtendedTemplate
    ExpTable->>SmTable: [tableData]=experiments() [columns]=tableCols()<br/>[selection] [checkedItems] [sortFields] [noDataTemplate]
    Note over ExpTable,SmTable: ng-template pTemplate="body/card/sort-filter/..." を<br/>コンテンツとして渡す（詳細は 03-02）
    Split->>Outlet: 右ペインに配置（[visible]=minimizedView()）
    EC->>Footer: [footerState] [footerItems]
    Note over EC,Footer: footerState$ は checkedExperiments が 2 件以上に<br/>なるまで値を流さないので、初期状態では非表示
```

## 読みどころ

- コンストラクタ本体: [experiments.component.ts:261](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/experiments.component.ts:261)
- 基底クラスのコンストラクタ: [base-entity-page.ts:119](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/shared/entity-page/base-entity-page.ts:119)
- 自動更新の購読（`refresh.tick`）: [base-entity-page.ts:135](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/shared/entity-page/base-entity-page.ts:135)
- ルート定義とプロバイダ: [experiment-routes.ts:27](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/experiment-routes.ts:27)
- テンプレート: [experiments.component.html](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/experiments.component.html)

`ExperimentsComponent` は `ChangeDetectionStrategy.OnPush` で、入力の多くは `| ngrxPush`（`@ngrx/component`）
または `store.selectSignal()` 経由で渡る。どちらも `async` パイプと違い、値が届いたときに
そのビューだけを明示的にマークするため、親コンポーネントの再評価を待たずに更新される。
