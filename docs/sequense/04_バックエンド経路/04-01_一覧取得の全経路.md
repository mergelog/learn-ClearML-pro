# 04-01 一覧取得の全経路

一覧データは `tasks.get_all_ex` 1 本で取る。
`ExperimentsComponent` は `getExperiments()` を発行するだけで、
クエリの組み立てに必要な 14 個の状態は Effect 側が Store から集める。

## 図1: Action から HTTP まで

```mermaid
sequenceDiagram
    autonumber
    participant EC as ExperimentsComponent
    participant Store
    participant ViewEff as ViewEffects
    participant TasksApi as ApiTasksService
    participant Http as SmApiRequestsService
    participant Server as apiserver

    EC->>Store: dispatch getExperiments()
    Store->>ViewEff: activeLoader effect → dispatch activeLoader(type)
    Store->>ViewEff: reFetchExperiment effect

    ViewEff->>ViewEff: refreshActions.push(action.type)
    ViewEff->>Store: select(selectActiveWorkspaceReady) が true になるまで待つ
    ViewEff->>ViewEff: auditTime(50)
    Note right of ViewEff: URL 復元で複数 Action が連続しても<br/>リクエストは 1 回にまとまる

    ViewEff->>ViewEff: fetchExperiments$(null, refreshScroll: true)
    ViewEff->>Store: projectId / archived / globalFilter / sortFields / tableFilters /<br/>selectedExperiments / showAllSelected / cols / metricCols /<br/>deep / showHidden / isCompare / isPipeline / isDataset
    ViewEff->>ViewEff: getGetAllQuery({...}) でリクエスト body を組む
    ViewEff->>TasksApi: tasksGetAllEx(query)
    TasksApi->>Http: post(`${basePath}/tasks.get_all_ex`, query, {withCredentials: true})
    Http->>Server: POST /tasks.get_all_ex
```

## 図2: 応答から画面反映まで

```mermaid
sequenceDiagram
    autonumber
    participant Server as apiserver
    participant Http as SmApiRequestsService
    participant ViewEff as ViewEffects
    participant Store
    participant EC as ExperimentsComponent
    participant ExpTable as sm-experiments-table

    Server-->>Http: {meta, data: {tasks, scroll_id}}
    Http->>Http: map(res => res.data)
    Http-->>ViewEff: {tasks, scroll_id}
    ViewEff->>ViewEff: convertStopToComplete(tasks)（status: 'closed' を 'completed' に読み替え）

    ViewEff->>Store: setNoMoreExperiments({hasMore: tasks.length が 30 未満か})
    ViewEff->>Store: setExperiments({experiments})
    ViewEff->>Store: setCurrentScrollId({scrollId})
    ViewEff->>Store: 溜めた refreshActions ぶんの deactivateLoader

    Store-->>EC: selectExperimentsList
    EC->>EC: filterArchivedExperiments(experiments, inArchivedMode())
    Note right of EC: アーカイブ直後の行を一覧から落とすための後処理
    EC->>ExpTable: [experiments]
    ExpTable->>ExpTable: roundedMetricValues を再計算
```

## エラー経路

```mermaid
sequenceDiagram
    autonumber
    participant Server as apiserver
    participant ViewEff as ViewEffects
    participant Store

    Server-->>ViewEff: HTTP エラー
    ViewEff->>Store: dispatch requestFailed(error)
    ViewEff->>Store: dispatch deactivateLoader(action.type)
    ViewEff->>Store: dispatch addMessage('warn', 'Fetch Tasks failed',<br/>[{name: 'More info', actions: [setServerError(...)]}])
    Note right of Store: リスト自体は前回の値を保持したまま
```

## クエリ組み立ての要点

`getGetAllQuery` が Store の状態をリクエスト body に変換する。

- `only_fields`: 表示中の列の `getter`（または `id`）だけを集める。列を増やすとリクエストのフィールドも増える
- `system_tags`: アーカイブ表示かどうか、pipeline / dataset / reports を除外するかをここで表現する
- `project`: `'*'`（全プロジェクト）のときは `undefined` にして絞り込みを外す
- `include_subprojects`: deep モードかつプロジェクトフィルタなしのときだけ true
- `id`: 「選択中のみ表示」が有効なら選択済み ID を入れる
- `type`: pipeline なら `Controller`、dataset なら `DataProcessing` に固定

## 読みどころ

- 取得の Effect: [common-experiments-view.effects.ts:240](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/effects/common-experiments-view.effects.ts:240)
- 状態の収集: [common-experiments-view.effects.ts:761](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/effects/common-experiments-view.effects.ts:761)
- クエリ組み立て: [common-experiments-view.effects.ts:964](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/effects/common-experiments-view.effects.ts:964)
- API サービス: [tasks.service.ts:126](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/business-logic/api-services/tasks.service.ts:126)
- HTTP 層: [api-requests.service.ts:26](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/business-logic/api-services/api-requests.service.ts:28)
- 一覧の後処理: [experiments.component.ts:207](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/experiments.component.ts:207)

ClearML の API は全エンドポイントが POST で、レスポンスは `{meta, data}` の形で返る。
`SmApiRequestsService.post` が `data` だけを取り出すため、
Effect からは `{tasks, scroll_id}` が直接見える。認証は `withCredentials: true` の Cookie による。
