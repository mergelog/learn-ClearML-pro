# 05-05 カードビューまでの経路

情報パネルを開くとテーブルが左に縮み、行がカードに変わる。その切り替わりと、カードの中身まで。

同じ `p-table` を使い続けていて、**`minimizedView` という1つの入力で
列の羅列（td × 列数）を1枚のカード（td × 1）に差し替えている**。

## 図1: 縮小表示に切り替わるまで

```mermaid
sequenceDiagram
    autonumber
    participant Store as NgRx Store
    participant EC as ExperimentsComponent
    participant ExpTable as sm-experiments-table
    participant SmTable as sm-table
    participant PTable as p-table

    Note over Store,EC: minimizedView は URL 由来<br/>selectMinimizedView = ルートパラメータに experimentId か ids があるか
    Store-->>EC: minimizedView()
    EC->>ExpTable: [minimizedView]=minimizedView() [cardHeight]=112<br/>[cardsCollapsed]=cardsCollapsed() [showColors]=showColorsInCards()
    ExpTable->>SmTable: [minimizedView] をそのまま流す
    SmTable->>PTable: class card-view / [virtualScrollItemSize]=cardHeight()
    SmTable->>SmTable: table() が生えたとき gotTable() → updateFilter()<br/>minimizedView なら p-table の _filter と sortSingle を差し替え
    Note right of SmTable: データ更新でページ位置が 0 に戻るのを防ぐための上書き
    SmTable->>SmTable: calcResize() → 幅は「1列ぶん」として再計算
```

`minimizedView` が URL 由来なのが重要。**情報パネルを開く＝URL に experimentId が載る＝
テーブルが縮む**、という一本道になっていて、パネルの開閉状態をどこかに別途持っていない。

## 図2: カード1枚の中身

```mermaid
sequenceDiagram
    autonumber
    participant PTable as p-table
    participant SmTable as sm-table
    participant ExpTable as sm-experiments-table
    participant Card as sm-table-card

    PTable->>SmTable: #body（行テンプレート）
    SmTable->>SmTable: minimizedView なので td を1個だけ作る
    SmTable->>ExpTable: ngTemplateOutlet cardTemplate<br/>context: rowData, rowNumber, selected
    ExpTable->>Card: [cardName] [columns] [rowData] [checked]<br/>[tagsTemplate]=tagList [compactColDataTemplate]=compactCol
    ExpTable->>Card: ng-content で差し込む部品を並べる
    Note over ExpTable,Card: sm-table-card は ng-content select= で<br/>mat-checkbox / sm-status-icon-label / [sm-name-icon] /<br/>[sm-name-version] / [sm-mini-tags] を所定の位置に置く
    Card->>Card: tagsTemplate と compactColDataTemplate は<br/>ngTemplateOutlet で自分の好きな場所に描く
```

カードは「input で渡す」「ng-content で差し込む」「ng-template を渡す」の**3方式が同時に使われている**
珍しい例。細かい使い分けは 03-04 を参照。

## 図3: カード表示時のヘッダー

縮小表示では列ヘッダーが1本の帯になり、そこにフィルタとソートが集まる。

```mermaid
sequenceDiagram
    autonumber
    participant PTable as p-table
    participant SmTable as sm-table
    participant ExpTable as sm-experiments-table
    participant CardFilter as sm-table-card-filter

    PTable->>SmTable: #header
    SmTable->>SmTable: minimizedView なので card-header を1行だけ描く
    SmTable->>ExpTable: ngTemplateOutlet checkboxTemplate（全選択）
    SmTable->>SmTable: 折りたたみボタン → (cardsCollapsedToggle)
    SmTable->>SmTable: sm-menu "SORTED BY"（sortableCols を列挙）
    SmTable->>ExpTable: ngTemplateOutlet cardHeaderTemplate<br/>（pTemplate="cardFilter" で渡したもの）
    ExpTable->>CardFilter: [options]=filtersOptions() [value]=filtersValues()<br/>[columns]=initialColumns()
    Note over ExpTable,CardFilter: 列ヘッダーが無いので、全列ぶんのフィルタを<br/>1つの mat-menu の入れ子で出す
```

## 読みどころ

- minimizedView の定義: [projects.reducer.ts:106](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/core/reducers/projects.reducer.ts#L106)
- td 1個に差し替える分岐: [table.component.html:160](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/shared/ui-components/data/table/table.component.html#L160)
- card テンプレート: [experiments-table.component.html:215](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/experiments/dumb/experiments-table/experiments-table.component.html#L215)
- カードヘッダー: [table.component.html:95](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/shared/ui-components/data/table/table.component.html#L95)
- PrimeNG 関数の上書き: [overrideFilterFunc.ts](/home/mtrysd/work_2026/000-learn-ClearML-pro/src/app/webapp-common/shared/ui-components/data/table/overrideFilterFunc.ts)
