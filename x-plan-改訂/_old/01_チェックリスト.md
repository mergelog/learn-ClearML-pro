# チェックリスト（データカタログ編）

作成日: 2026-09-12
対になる資料: `x-plan-改訂/00_プラン.md`（理由と背景はそちらだけが持つ）

**この表は状態だけを持つ。** 理由を書き写すと 00 と二重管理になって必ずずれる。

**記号**: ✅完了 / 🔄着手中 / ⬜未着手 / ⏸トリガー待ち / ⛔着手しない（決定済み）

---

## サマリ

| Stage | 内容 | 項目数 | 完了 | 状態 |
| --- | --- | ---: | ---: | --- |
| N | データカタログ（自作 feature 新設） | 11 | 11 | ✅ |
| K | 既存 ClearML feature への最小改造 | 3 | 3 | ✅ |
| P | ベストプラクティス点検 | 7 | 7 | ✅ |
| E | 外部への提供口 | 3 | 3 | ✅ |

---

## Stage N — データカタログ

| ID | 項目 | 対象 | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| N-0 | ゲートの受け皿を先に作る（`tsconfig.strict.json` の `include` / `test-pyramid.json` の `web-unit` に `features/data-catalog/**` を追加） | `apps/web/tsconfig.strict.json` / `test-pyramid.json` | － | ✅ | 2026-09-12 | `web-unit-vendor` の `exclude` にも同じ行が要った。片方だけだと同じ spec が2層で数えられる |
| N-1 | `CatalogAsset` の型と語彙を決める | `features/data-catalog/data-catalog.model.ts` | N-0 | ✅ | 2026-09-12 | 7項目に留めた。`description` は詳細（`CatalogAssetDetail`）へ。一覧の判断に要らないものを行数ぶん運ばせない |
| N-2 | `data-access`（api.service + adapter） | `data-access/data-catalog-api.service.ts` / `.adapter.ts` | N-1 | ✅ | 2026-09-12 | 生成要求型に無い条件（`last_update`・入れ子フィールド）は `withExtraCriteria` の1箇所だけで外す |
| N-3 | NgRx feature state（actions / reducer / selectors / effects） | `state/**` | N-2 | ✅ | 2026-09-12 | `filterChanged`（URLへ書く）と `openList`（URLを読んで引く）を分けた。経路を1本に保つため |
| N-4 | 一覧画面（container + presentational table） | `containers/` / `components/` | N-3 | ✅ | 2026-09-12 | 3種別を別々に引いて更新日時順に混ぜる。混ぜる先はClearMLに無いのでここだけ手元 |
| N-5 | 検索とフィルタ（種別 / プロジェクト / タグ / 期間）と URL クエリ同期 | `containers/` / `state/` | N-4 | ✅ | 2026-09-12 | 変換は `data-catalog.query.ts` に独立させた。往復で同じになることを spec で固定 |
| N-6 | 詳細（メタデータ表示・既存 ClearML 画面への受け渡し） | `containers/` / `components/` | N-4 | ✅ | 2026-09-12 | **Stage E のトリガーが引かれた**。URL だけで開ける（`/data-catalog/:kind/:id`） |
| N-7 | lineage（Dataset → Run → Model）の表示 | `data-access/` / `components/` | N-6 | ✅ | 2026-09-12 | Dataset版数の読み取りと `describe`（失敗の文言化）の2つが重複した。寄せる判断は P-2 |
| N-8 | メタデータ編集（tags / description） | `state/` / `data-access/` | N-6 | ✅ | 2026-09-12 | ADR 008。楽観更新をしない（巻き戻し不要）／二重編集は防がず隠さない |
| N-9 | ルーティングと feature 局所 provider | `data-catalog.routes.ts` | N-3 | ✅ | 2026-09-12 | provider は親に、`data` は子ごとに。`app.routes.ts` へ1行追加（記録は K-3） |
| N-10 | 検証（spec 一式 + E2E smoke 1 + a11y 1） | `**/*.spec.ts` / `apps/web/e2e/**` | N-4〜N-9 | ✅ | 2026-09-12 | 単体 270→389（自作 web-unit 281）。E2E 27→29（上限30） |

**完了条件**: `pnpm run verify:web` と `pnpm run web:e2e` が緑。
かつ `pnpm run test:pyramid` / `web:typecheck:strict` が新 feature を数えていること。

---

## Stage K — 既存 ClearML feature への最小改造

| ID | 項目 | 対象 | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| K-1 | side-nav にカタログ導線を追加 | `layout/side-nav/side-nav.component.html` | N-9 | ✅ | 2026-09-12 | quality-pipeline の直下に1件（`al-ico-data-audit`）。`aria-label` があるので `elements-content` は増えない |
| K-2 | Dataset / Model 側からカタログへの相互リンク | 該当するベンダー feature（必要箇所のみ） | N-6 | ✅ | 2026-09-12 | 2箇所（model-info-header / open-dataset-version-details）。`RouterLink` を `imports` へ足す分だけ `.ts` も触る |
| K-3 | 凍結領域へ入れた例外の記録 | `apps/web/README.md` | K-1, K-2 | ✅ | 2026-09-12 | 「凍結領域へ入れた例外」節を新設。`app.routes.ts` の1行（N-9）もここで記録。boundaries / baseline の更新は不要だった |

**完了条件**: `pnpm run web:boundaries` / `web:lint:baseline` が緑。README に例外の一覧がある。

---

## Stage P — ベストプラクティス点検

| ID | 項目 | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- |
| P-1 | ADR 007（feature 構成）との突き合わせ。ずれたら ADR を直す | N-10 | ✅ | 2026-09-12 | ずれは無し。2本目で初めて分かった3点を ADR 007 へ**足した**（`initialValue` は型で決まる／provider は state の寿命で決まる／失敗の重さは「操作が成立したか」） |
| P-2 | 旧 D-3。`shared/` へ寄せるものの判断（§4.3 の lineage 読み取り） | N-7 | ✅ | 2026-09-12 | 寄せたのは失敗の文言づくりだけ（`shared/clearml/`）。Dataset 版数の読み取りは寄せない。基準は「判断か、事実か」（ADR 009） |
| P-3 | 旧 D-5。`libs/` を切るかの再判定 | P-2 | ✅ | 2026-09-12 | **切らない**。3トリガーとも実測で引かれていない。次に見直すのは②（`shared/` の自作ファイル数）（ADR 009） |
| P-4 | 旧 D-4。`@defer` と初期表示バジェットの実測 | N-10 | ✅ | 2026-09-12 | **入れない**。`data-catalog` の初期束への寄与は 0。ただし初期束の余裕は 0.05 MB（1.6%）しかない（ADR 009） |
| P-5 | 旧 D-1。zoneless のトリガー再評価 | N-10 | ✅ | 2026-09-12 | **動かさない**。自作分はすでに zoneless 相当で、費用は凍結領域に出る。トリガー②を `.subscribe(` 依存数へ差し替え（ADR 009） |
| P-6 | lint / baseline（自作分は baseline に入れず直す） | N-10 | ✅ | 2026-09-12 | 自作分は baseline に0件。`data-catalog` 全体で 0 problems。`@ngrx/*` 31ルールが効いていることを実地で確認 |
| P-7 | `test-pyramid.json` / `ci:parity` の整合 | N-10 | ✅ | 2026-09-12 | 直したのは宣言の側。`e2e.maxReason` を 27件→**29件**へ。**残り枠は1件** |

**完了条件**: `pnpm run verify:web` / `ci:parity` / `test:pyramid` が緑。
P-2 / P-3 / P-5 は**判断を書いて閉じる**（実装を伴わなくてよい）。

---

## Stage E — 外部への提供口（**N-6 完了でトリガーが引かれた**）

| ID | 項目 | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- |
| E-1 | 何をどの形で外へ出すかの決定（ADR） | N-6 | ✅ | 2026-09-12 | ADR 010。**E-b と E-a を採り、E-c（HTTP API）は採らない**（消費者ゼロ・ClearML 読み取りの二言語重複）。再検討のトリガー3つを記録 |
| E-2 | 決めた出口の実装 | E-1 | ✅ | 2026-09-12 | E-b は N-5 で実体があり、足したのは `toReferenceUrl` の1本だけ。E-a は**封筒つき JSON**（`filter` / `reference` / `truncated`）。CSV は出さない |
| E-3 | 出口の検査（破壊変更検知・認可） | E-2 | ✅ | 2026-09-12 | `data-catalog.surface.published.json` と完全一致で検知し、分類は説明に使う（ADR 006 §1 と同じ）。認可は**増えない**（新しい問い合わせ経路が0本） |

**完了条件**: `pnpm run verify:web` が緑。公開している面がコミットされていて、
黙って変わらないこと。

---

## 実施順序

```text
N-0（受け皿）
  ↓
N-1 → N-2 → N-3 → N-9 ──┬─→ K-1
                        ↓
                      N-4 → N-5
                        ↓
                      N-6 ──┬─→ N-7 ──→ P-2 → P-3
                            ├─→ N-8
                            ├─→ K-2 → K-3
                            └─→ E-1 → E-2 → E-3
                        ↓
                      N-10 ─┬─→ P-1 / P-4 / P-5 / P-6 / P-7
```

**N-0 が全ての前提。** 検査されない場所へコードを足しても緑になるだけである。

---

## 検証コマンド

```bash
pnpm run verify:web              # 各 Stage はこれが緑になることで閉じる

pnpm run web:gates:test          # ゲートスクリプト自体のテスト
pnpm run ci:parity               # ci.yml が verify を本当に走らせているか
pnpm run test:pyramid            # テストが決めた層に置かれているか
pnpm run web:lint:baseline       # ESLint 違反が baseline を超えていないか
pnpm run web:boundaries          # レイヤ違反（feature間 / 生成API）
pnpm run web:typecheck:strict    # strict 対象範囲の型検査
pnpm run web:test                # Vitest
pnpm run web:scan:build          # バンドル実測（P-4）
pnpm run web:e2e                 # Playwright
```

Angular を起動して見る場合は `ng serve --host 0.0.0.0 --port 4200`。

---

## 更新ルール

1. **この表では状態・完了日・実測値だけを更新する。** 方針が変わったら `00_プラン.md` を直し、ここには結果だけを反映する
2. 完了にするのは**検証が緑になってから**。「変更した」ではなく「検証が通った」が完了の定義
3. 途中で分かったことは**備考欄に一行**。詳細は `00_プラン.md` か `apps/web/README.md` / ADR へ。**この表を厚くしない**
4. サマリの件数は、表を更新したら合わせて直す
5. `x-plan-改訂/_old/` と `x-old-plan/` は更新しない。履歴として凍結する

---

## 作業証跡（1作業ごとに追記）

```text
日付: 2026-09-12
対象ID: N-0 〜 N-10（Stage N 全項目）
今回の範囲: 自作 feature `data-catalog` の新設。受け皿・型・data-access・state・
  一覧・検索とURL同期・詳細・lineage・メタデータ編集・ルーティング・検証
対象外: Stage K（side-nav 導線・相互リンク・例外の記録）、Stage P、Stage E
完了条件: `pnpm run verify:web` と `pnpm run web:e2e` が緑。
  `test:pyramid` / `web:typecheck:strict` が新 feature を数えていること

やったこと:
  N-0  tsconfig.strict.json の include と test-pyramid.json の web-unit へ
       `features/data-catalog/**` を追加。web-unit-vendor の exclude にも同じ行を足した
  N-1  data-catalog.model.ts（CatalogAsset 7項目 / Detail / Lineage / Filter / State）
  N-2  data-catalog.adapter.ts + data-catalog-api.service.ts
  N-3  state/（actions / reducer / selectors / effects）
  N-4  containers/data-catalog-page + components/catalog-assets-table
  N-5  components/catalog-filters + data-catalog.query.ts（URL との相互変換）
  N-6  containers/catalog-asset-detail-page + components/catalog-asset-facts
  N-7  components/catalog-lineage + api.service の lineage 3経路
  N-8  components/catalog-metadata-form + updateMetadata。ADR 008 を起こした
  N-9  data-catalog.routes.ts（provider は親・data は子）。app.routes.ts へ1行
  N-10 spec 12本 + E2E smoke 1 + a11y 1 + e2e/fixtures/data-catalog.fixture.ts

読んだ主要ファイル:
  features/quality-pipeline/**（形の手本）、apps/web/web-boundaries.json、
  test-pyramid.json、apps/web/tsconfig.strict.json、docs/adr/007、
  business-logic/model/tasks/task.ts・models/model.ts・*GetAllExRequest.ts、
  webapp-common/dataset-version/**（Dataset の版数の置き場所）、
  tools/semiconductor_seed/clearml_gateway.py（Dataset の登録のされ方）

確認した処理経路:
  route（queryParams）→ openList → api.search → 3種別を forkJoin → 混ぜて1ページ
  → 一覧 → 資産のリンク → route（paramMap）→ openDetail → getDetail
  → detailLoaded → getLineage → lineageLoaded
  保存: saveMetadata → updateMetadata（updated===1 のみ成功）→ metadataSaved
  → openDetail（読み直し）

自分で加えた変更:
  apps/web/src/app/features/data-catalog/**（45ファイル・新規）
  apps/web/e2e/data-catalog.spec.ts / e2e/fixtures/data-catalog.fixture.ts（新規）
  apps/web/e2e/accessibility.spec.ts（a11y 1本追加。violationsOf を画面引数に）
  apps/web/src/app/app.routes.ts（`data-catalog` の1エントリ。凍結領域の例外→K-3 で記録）
  apps/web/tsconfig.strict.json / test-pyramid.json（N-0）
  apps/web/README.md（自作 feature が2つになった旨と、data-catalog の2つの追加）
  docs/adr/008_20260912_catalog_metadata_editing.md（新規）

追加したテスト:
  web-unit 12ファイル（query / adapter / api.service / reducer / selectors / effects /
  component 5 / container 2）。281ケース（feature 全体）
  e2e 2ケース（data-catalog smoke 1 / accessibility 1）

意図的に壊したテストと確認結果:
  - `toModelState` を常に 'completed' にした → adapter spec の
    「ready でないものを draft と呼ぶ」1本が落ちた
  - reducer の saveMetadata で detail を先に書き換えた（楽観更新）→
    「押しても表示は変わらない」1本が落ちた（388 passed / 1 failed）
  - CatalogAssetsTable の @empty を「No assets.」1文にまとめた →
    「まだ無い」「条件に合わない」「読み込み中」の3本が落ちた（386 passed / 3 failed）
  いずれも戻して 389 passed を確認。

実行した確認コマンドと結果:
  pnpm run verify:web            → 緑（exit 0）
  pnpm run web:e2e               → 29 passed
  pnpm run test:pyramid          → 141ファイルすべて宣言どおり
  node scripts/test-pyramid.mjs report → web-unit 24ファイル281ケース / e2e 6ファイル29ケース（上限30）
  pnpm run web:typecheck:strict  → 移行済みの範囲にエラーなし
  pnpm run web:lint:baseline     → errors 3198/3198・warnings 1087/1087（新規違反なし）
  pnpm run web:boundaries        → 新しい違反なし（既知 217 件）

分かったこと:
  - ClearML の Dataset 版数は Task の `runtime.version` にある。学習Run が使った
    版数は hyperparams の `Dataset/dataset_version`。**置き場所が違う**ので、
    lineage の順方向と逆方向で読む場所が変わる
  - 生成された要求型（`TasksGetAllExRequest` 等）は ClearML が受け付ける条件より狭い。
    `tasks.get_all_ex` の `last_update` や入れ子フィールド指定は型に無い
  - `last_update` の範囲は2要素の配列ではなく、比較記号を前置した文字列の並び
    （`['>=2026-01-01', '<=2026-09-12']`）。`modelsGetAllExRequest` の註釈が正
  - `toSignal` の `initialValue` は本来の型でなければならない。`ParamMap` に
    「まだ無い」を表す値は無いので、初期値を置かず `undefined` のまま扱う
  - `DatePipe` の `z` は `UTC` ではなく `GMT+0` を出す（既存 spec も `GMT` で見ている）
  - 重複が2つ出た: Dataset版数の読み取りと、失敗の文言化（`describe`）。
    どちらも **寄せずに2箇所目を書いた**。P-2 で見比べる

次の一手:
  K-1（side-nav にカタログ導線）→ K-2 → K-3（app.routes.ts の例外を README へ記録）。
  そのあと P-1 / P-4 / P-6 / P-7、N-7 を材料に P-2 → P-3。
  E-1 はトリガーが引かれた状態で未着手。
```

```text
日付: 2026-09-12
対象ID: K-1 / K-2 / K-3（Stage K 全項目）
今回の範囲: 既存 ClearML 画面への導線と相互リンク、および凍結領域へ入れた例外の記録
対象外: Stage P、Stage E
完了条件: `pnpm run web:boundaries` / `web:lint:baseline` が緑。README に例外の一覧がある

やったこと:
  K-1  side-nav に `/data-catalog` を1件（`al-ico-data-audit` /
       `data-id="dataCatalogIcon"` / `aria-label="Data catalog"`）。
       quality-pipeline の直下に置いて、自作の2画面を隣同士にした
  K-2  Model 側: `model-info-header.component.html` の ID バッジの隣に
       アイコンリンク1件（`/data-catalog/model/:id`）。既存の
       `@if (model()?.id)` を `as modelId` に変えて条件の二重書きを避けた
       Dataset 側: `open-dataset-version-details.component.html` の
       VERSION INFO の ID 行の下に1行（`/data-catalog/dataset/:id`）
       どちらも `.ts` は `RouterLink` を `imports` へ足すだけ
  K-3  `apps/web/README.md` に「凍結領域へ入れた例外（2026-09-12 時点）」節。
       K-1 / K-2 の4ファイルに加えて、N-9 で触った `app.routes.ts` も表に入れた

読んだ主要ファイル:
  layout/side-nav/side-nav.component.html・.ts・.scss（.scss は空）、
  webapp-common/models/dumbs/model-info-header/**、
  webapp-common/dataset-version/open-dataset-version-details/**、
  webapp-common/assets/fonts/trains-icons.scss（使えるアイコン名の確認）、
  apps/web/web-boundaries.json、apps/web/eslint-baseline.json

確認した処理経路:
  side-nav → `/data-catalog` → 一覧
  Model 詳細 → `/data-catalog/model/:id` → 詳細 → `catalogVendorLink` → Model 詳細
  Dataset 版数 → `/data-catalog/dataset/:id` → 詳細 → `catalogVendorLink` → Dataset 版数
  （往復が閉じている。台帳は横断の入口であって置き換えではない）

自分で加えた変更:
  apps/web/src/app/layout/side-nav/side-nav.component.html
  apps/web/src/app/webapp-common/models/dumbs/model-info-header/model-info-header.component.html / .ts
  apps/web/src/app/webapp-common/dataset-version/open-dataset-version-details/open-dataset-version-details.component.html / .ts
  apps/web/README.md（例外の節を新設）

追加したテスト:
  無し。E2E は 29/30 で予備が3件しかなく、**導線1本のために予備を使わない**と判断した。
  リンク先の正しさは `data-catalog.spec.ts` の `catalogVendorLink` が逆方向で
  既に押さえている（同じ URL の形を両端から見ている）

意図的に壊したテストと確認結果:
  - 新しい `<mat-icon ...></mat-icon>` を閉じタグで書いた（自然な書き方であり、
    周りの既存行もそう書いている）→ `prefer-self-closing-tags` が
    1031→1032 に増えて `web:lint:baseline` が落ちた。self-closing に直して戻した。
    **ベンダーファイルの中でも、新しく足す行は現行のルールに従う**（README に明記）

実行した確認コマンドと結果:
  pnpm run verify:web            → 緑（exit 0）
  pnpm run web:lint:baseline     → errors 3198/3198・warnings 1087/1087（新規違反なし）
  pnpm run web:boundaries        → 新しい違反なし（既知 217 件）
  pnpm run web:test              → 73ファイル 389 passed（Stage N から増減なし）
  pnpm run web:e2e               → 29 passed
  pnpm run test:pyramid          → 141ファイルすべて宣言どおり

分かったこと:
  - side-nav の `<a>` は `elements-content`（中身が無い）で既に6件落ちている。
    `aria-label` を付けたものは対象外になるので、**K-1 は errors を増やさない**。
    quality-pipeline の行が `aria-label` を持っているのは、たまたまではなく
    このため（同じ形を踏襲した）
  - `a[mat-icon-button]` は `MatIconAnchor` だが、これは `MatIconButton` の別名で
    セレクタが `a` も含む。既に import 済みなので `imports` の追加は要らなかった
  - 凍結領域の改造で衝突が問題になるのは「同じ行を両側が変えた」ときだけである。
    **追加だけに留めれば**、追従時の解決は機械的に済む。K-2 で既存の
    `@if (model()?.id)` を `as modelId` に変えたのが唯一の書き換えで、
    これは条件を二重に書かないための1行に収めた

次の一手:
  Stage P。P-1（ADR 007 との突き合わせ）/ P-4（`@defer` とバジェット実測）/
  P-6（lint・baseline）/ P-7（`test-pyramid.json` と `ci:parity` の整合）、
  N-7 を材料に P-2 → P-3、P-5（zoneless）。
  Stage E-1 はトリガーが引かれた状態で未着手。
```

```text
日付: 2026-09-12
対象ID: P-1 / P-2 / P-3 / P-4 / P-5 / P-6 / P-7（Stage P 全項目）
今回の範囲: 2本目の feature が出たことで判定できるようになった4つの保留（共有層・
  `libs/`・`@defer`・zoneless）の実測と、ADR / lint / テストの層の宣言との突き合わせ
対象外: Stage E
完了条件: `pnpm run verify:web` / `ci:parity` / `test:pyramid` が緑。
  P-2 / P-3 / P-5 は判断を書いて閉じる（実装を伴わなくてよい）

やったこと:
  P-1  ADR 007 を点検。4つの決定すべてに沿っていた（`NgModule` 0件 /
       `selectSignal` 15箇所・`subscribe` 0件 / provider は `data-catalog.routes.ts`
       の1箇所 / `catchError` はすべて action か `EMPTY`）。沿っていたので
       **直さず、足した**。失敗の表は quality-pipeline 固有の項目名だったので、
       基準（利用者の操作が成立したか）を先に書いて両 feature 版へ直した
  P-2  `shared/clearml/clearml-failure.ts` を新設し、両 Effects から
       `describe` / `describeHttpFailure` / `UNEXPLAINED_FAILURE` を削った
       （quality-pipeline −54行・data-catalog −50行）。lineage の読み取りは寄せない
  P-3  `libs/` を切らないと判定。ADR 009 §3 に3トリガーの実測表を残した
  P-4  `pnpm run web:build` / `web:scan:build` で実測して `@defer` を入れないと判定
  P-5  凍結領域の zone 依存を実測して動かさないと判定。トリガー②を差し替えた
  P-6  `data-catalog` 全体（`.ts` / `.html`）に ESLint → 0 problems。
       `eslint-baseline.json` に自作 feature の文字列は0件
  P-7  `test-pyramid.json` の `e2e.maxReason`（「現在27件」の古い記述）を29件へ。
       `web-unit` の `verifies` に共有層を追記

読んだ主要ファイル:
  docs/adr/007（突き合わせの対象）、docs/adr/003（凍結方針）、docs/adr/005（層）、
  features/quality-pipeline/state/quality-pipeline.effects.ts（寄せる元）、
  features/data-catalog/state/data-catalog.effects.ts（寄せる元）、
  apps/web/eslint-baseline.json、test-pyramid.json、apps/web/tsconfig.strict.json、
  apps/web/README.md

自分で加えた変更:
  新規 apps/web/src/app/shared/clearml/clearml-failure.ts（寄せた実体）
  新規 apps/web/src/app/shared/clearml/clearml-failure.spec.ts（8ケース）
  新規 docs/adr/009_20260912_shared_layer_and_deferred_decisions.md
  変更 features/quality-pipeline/state/quality-pipeline.effects.ts（−54行）
  変更 features/data-catalog/state/data-catalog.effects.ts（−50行）
  変更 apps/web/tsconfig.strict.json（`include` に `src/app/shared/clearml/**/*.ts`）
  変更 test-pyramid.json（`web-unit` の `include` と `web-unit-vendor` の `exclude`
       の**両方**／`e2e.maxReason` 27→29件／`web-unit` の `verifies`）
  変更 docs/adr/007_20260912_angular_feature_composition.md（点検結果を追記）
  変更 apps/web/README.md（`shared/clearml` を構成図へ・受け皿3か所・ADR 009 リンク）

追加したテスト:
  apps/web/src/app/shared/clearml/clearml-failure.spec.ts の8ケースのみ。
  単体 389→397（+8）。既存の spec は1本も増減していない。
  E2E は増やしていない（29/30 のまま。残り枠1件）

意図的に壊したテストと確認結果:
  - 共有層から状態コード0の分岐を消した → 3 failed（共有 spec 1 + 両 feature の
    effects spec 各1）。**寄せた先が両方から見られていることの確認**。戻して 397 passed
  - `HttpErrorResponse` と `Error` の判定順を入れ替えた → **落ちない**。
    寄せる元の註釈（「先に見ないと通信の失敗が固定文言へ落ちる」）が**間違っていた**。
    `HttpErrorResponse` は `Error` を implements しているだけで継承していないので
    `instanceof Error` に掛からない。共有層では註釈を事実に直した
    （順序自体は「狭いほうから見る」という並びのため維持）
  - `selectCanSave` → `canSave` に改名 → `@ngrx/prefix-selectors-with-select` が
    1 error。戻して 0 に復帰。**`@ngrx/eslint-plugin` が新 state に効いている**

実行した確認コマンドと結果:
  pnpm run web:gates:test        → 緑（fail 0）
  pnpm run ci:parity             → 緑（手元の検証16件すべてが ci.yml で走る）
  pnpm run test:pyramid          → 緑（142ファイルすべて宣言どおり）
  pnpm run web:lint:baseline     → errors 3198/3198・warnings 1087/1087（新規違反なし）
  pnpm run web:boundaries        → 新しい違反なし（既知 217 件）
  pnpm run web:contract          → 6件すべて Python 側と一致
  pnpm run web:typecheck:strict  → 移行済み範囲にエラーなし（範囲外の残件 365 件）
  pnpm run web:test              → 74ファイル 397 passed（+ report-widgets 3 passed）
  pnpm run web:e2e:typecheck     → 緑
  pnpm run web:build             → 初期束 3.10 MB / 転送見積 796.18 kB
  pnpm run web:scan:build        → credential 検出なし
  pnpm run web:e2e               → 29 passed（38.3s・exit 0）
  pnpm run verify:web            → **1コマンドの通しで緑**（上記11工程すべて）

層別の実測（test-pyramid report）:
  gates 7/62・ml-unit 38/820・services-unit 11/169・tools-unit 5/80・
  web-unit 25/289・web-unit-vendor 50/111・e2e 6/29（上限30）

分かったこと:
  - **`shared/` へ自作コードを置くのは、feature を足すのと同じ取りこぼし方をする。**
    受け皿が3か所（`tsconfig.strict.json` の `include`、`test-pyramid.json` の
    `web-unit` `include` と `web-unit-vendor` `exclude`）要る。片方だけだと
    spec がベンダーの層として数えられ、strict 検査からも外れる。**Stage N-0 と同じ罠**
  - **重複は「同じ文字列があること」ではなく「同じ判断が2つあること」を指す。**
    失敗の文言は判断なので寄せた。版数の置き場所は ClearML 側の事実なので寄せない
  - **`@defer` も zoneless も、効かない場所に入れるところだった。** どちらも
    費用が出るのは凍結領域で、自作分をいくら削っても初期束も変更検知も変わらない。
    **測ってから決めたことが、そのまま「やらない」という結論になった**
  - **無意味な指標をトリガーに置いていた。** zoneless の②「`webapp-common` の
    OnPush 比率」は、Angular 22 で OnPush が既定である以上ほぼ 100% で動かない。
    `.subscribe(` 依存数へ差し替えた。**トリガーは、動く数で書かなければならない**
  - `data-catalog` の遅延 chunk を数えるとき `grep data-catalog` では拾いすぎる。
    K-2 で `RouterLink` を足したベンダー component の chunk にも文字列が入る。
    feature の chunk を数えるなら component 名やサービス名で引くこと
  - **E2E の残り枠は1件しかない。** 上限に触れる前に ADR 005 の問い
    （下の層でやるべきことを上げていないか）を必ず通すこと

次の一手:
  Stage E-1（外部への提供口の ADR）。N-6 完了でトリガーは引かれており、
  P-3 で `libs/` を切らないと決めたので、**出口は `apps/web` の中か `services/` 側かの
  二択**に絞られている（候補 E-a / E-b / E-c は `00_プラン.md` §5）。
  出すのは `CatalogAsset` であって生の API 応答ではない（E-2 の備考）
```

```text
日付: 2026-09-12
対象ID: E-1 / E-2 / E-3（Stage E 全項目）
今回の範囲: 台帳を外へ渡す口。何を出すかの決定（ADR 010）、参照URLと書き出しの
  実装、公開している面が黙って変わらないことの検査
対象外: 無し（Stage N / K / P / E がすべて閉じた）
完了条件: `pnpm run verify:web` が緑。公開している面がコミットされていること

やったこと:
  E-1  ADR 010。**E-b（参照URL）と E-a（書き出し）を採り、E-c（HTTP API）は
       採らない**。先に消費者を数えたのが判断の分かれ目で、E-b と E-a には
       いて、E-c にはいなかった（ADR 006 が `prediction_api` でやったのと
       同じ数え方）。E-c を再検討するトリガー3つを ADR に残した
  E-2  E-b は N-5 で実体ができていたので、足したのは `toReferenceUrl`（条件を
       人に渡せる1本のURLにする）だけ。E-a は `data-catalog.export.ts`
       （封筒 + `CatalogAsset`）と `data-catalog.download.ts`（ブラウザ境界）、
       一覧画面の Export ボタン
  E-3  `data-catalog.surface.ts` が公開している面を組み立て、
       `data-catalog.surface.published.json`（コミット済み）と完全一致するかを
       spec が見る。差分は「渡した先が壊れる変更」と「互換の変更」に分けて出す

読んだ主要ファイル:
  docs/adr/006（守り方の手本）、docs/adr/002（認可）、docs/adr/009（`libs/` の判断）、
  services/prediction_api/contract.py（`drift()` の形）、
  services/contracts/openapi.py、scripts/pipeline-contract.mjs、
  features/data-catalog/data-catalog.query.ts / .consts.ts / .model.ts、
  test-pyramid.json、apps/web/web-boundaries.json

確認した処理経路:
  一覧 → Export → `catalogExportDocument`（state の filter / assets / hasMore）
  → `catalogExportText` → `CatalogDownloadService.save` → ファイル
  ファイルの `reference` → `/data-catalog?...` → `fromQueryParams` → 同じ条件
  （**書き出したファイルから、それを作った画面へ戻れる**ことを spec で固定した）

自分で加えた変更:
  新規 docs/adr/010_20260912_catalog_external_surface.md
  新規 features/data-catalog/data-catalog.export.ts（+ spec）
  新規 features/data-catalog/data-catalog.download.ts（+ spec）
  新規 features/data-catalog/data-catalog.surface.ts（+ spec）
  新規 features/data-catalog/data-catalog.surface.published.json
  変更 features/data-catalog/data-catalog.query.ts（`toReferenceUrl` を追加）
  変更 features/data-catalog/data-catalog.routes.ts（`CatalogDownloadService` を provider へ）
  変更 containers/data-catalog-page/*（Export ボタン・打ち切りの注記・書き出しの組み立て）
  変更 apps/web/README.md（追加3行と、公開している面の説明）

追加したテスト:
  web-unit 3ファイル（export 10 / surface 8 / download 2）+ 既存2ファイルへ
  10ケース（query 5 / 一覧 container 5）。単体 397→427（+30）
  **E2E は増やしていない**（29/30 のまま。残り枠1件）。出口の判断はすべて
  下の層で見える形にしてある（ADR 005）

意図的に壊したテストと確認結果:
  - `QUERY_KEYS.text` を `'q'` → `'query'` に変えた → 4 failed。うち面の検査は
    `reference.queryKeys.text: "q" が "query" になった` を**「渡した先が壊れる変更」
    の側に**出した。註釈しか無かったときは緑だった変更である
  - 封筒の `truncated` を常に `false` にした → 2 failed（export spec と container
    spec）。**打ち切られた抜粋が台帳として渡る**変更が、両側から落ちる
  - 分類器で「並びから消えた」を互換扱いにした → 1 failed。検知（完全一致）は
    落ちたままなので、**分類を間違えても見逃しにはならない**ことも同時に確認した
  いずれも戻して 427 passed。

実行した確認コマンドと結果:
  pnpm run verify:web            → **緑（exit 0）**
  pnpm run web:test              → 77ファイル 427 passed（+ report-widgets 3 passed）
  pnpm run web:e2e               → 29 passed（37.1s）
  pnpm run test:pyramid          → 緑（145ファイルすべて宣言どおり）
  node scripts/test-pyramid.mjs report → web-unit 28ファイル319ケース / e2e 6ファイル29件
  pnpm run web:lint:baseline     → errors 3198/3198・warnings 1087/1087（新規違反なし）
  pnpm run web:boundaries        → 新しい違反なし（既知 217 件）
  pnpm run ci:parity             → 手元の検証16件すべてが ci.yml で走る
  eslint（data-catalog 全体）    → 0 problems
  pnpm run web:build             → 初期束 3.10 MB / 転送見積 796.15 kB（Stage P から動かず）

分かったこと:
  - **出口を決める前に消費者を数えると、作らない判断が先に出る。** 3候補のうち
    実際に呼び手がいたのは2つで、残る1つ（HTTP API）は ClearML 読み取りを
    TypeScript と Python の2言語へ複製することになる。ADR 009 で「事実の読み取りは
    寄せない」と決めた重複が、言語をまたぐと桁違いに高くつく
  - **CSV を出さないのは、封筒を入れる場所が無いからである。** 一覧は50件で
    打ち切るが、画面の注記はファイルに付いてこない。`truncated` を名乗れない形式で
    出すと、抜粋が台帳として読まれる。表計算の要求は正当なので、出てから
    「何を保証しなくなるか」を書いて足す（ADR 010 未決）
  - **註釈は検査ではない。** `QUERY_KEYS` には最初から「外向きの名前である」と
    書いてあった。書いてあることと守られていることの差は、破壊してみるまで
    出てこない
  - **外向きの口をブラウザの中に留めたので、ADR 002 の役割が1つも増えなかった。**
    新しい問い合わせ経路が0本だからである。E-c を採っていれば「台帳を読めるだけの
    役割」が要った。認可を増やさない最良の方法は、経路を増やさないことである
  - 面のスナップショットは `openapi.json`（472行）と違って40行しかない。
    生成の npm script を作らず、**失敗したテストが次の内容をそのまま出す**形にした。
    ゲートも `ci:parity` の対象も増えていない（ADR 006 §3 と同じ理由）

次の一手:
  **Stage N / K / P / E がすべて閉じた。** 残っているのは判断ではなく、
  トリガー待ちのものだけである。
  - E-c（カタログ参照の HTTP API）: ADR 010 §3 の①②③
  - `libs/` を切る: ADR 009 §3 のトリガー②（`shared/` の自作ファイル数）
  - zoneless: ADR 009 のトリガー②（`.subscribe(` 依存数）
  - `@defer`: 初期束の余裕は 0.05 MB（1.6%）のまま
  E2E の残り枠は1件。上限に触れる前に ADR 005 の問いを通すこと。
```
