# チェックリスト（統合版）

作成日: 2026-09-10
対になる資料: `x-plan-改訂/00_プラン.md`（理由と背景はそちらだけが持つ）

**この表は状態だけを持つ。** 理由を書き写すと 00 と二重管理になって必ずずれる。

**記号**: ✅完了 / 🔄着手中 / ⬜未着手 / ⏸トリガー待ち / ⛔着手しない（決定済み） / 🚧保留

---

## サマリ

| Stage | 内容 | 項目数 | 完了 | 状態 |
| --- | --- | ---: | ---: | --- |
| Stage 1 | Angular をベストプラクティスへ | 30 | 25 | ✅ A〜C 完了（D はトリガー待ち） |
| Stage 2 | 機械学習・MLOps の残り | 5 | 1 | 🔄 W12 完了（内訳10項目すべて）。次は W14 / W15 / W16 |
| Stage 3 | 既存 ClearML コードの読解（Level 3 へ） | 6 | 0 | 🔄 P0-0 が Level 2。次は P0-1 |

| Stage 1 内訳 | 項目数 | 完了 | 状態 |
| --- | ---: | ---: | --- |
| Z. 完了済み（旧プラン由来） | 6 | 6 | ✅ |
| A. 実害があるもの（P0） | 7 | 7 | ✅ |
| B. ルールの機械化（P1） | 6 | 6 | ✅ |
| C. 構造とドキュメント（P2） | 6 | 6 | ✅ |
| D. トリガー待ち（P3） | 5 | － | ⏸ |

---

## Stage 1 — Angular をベストプラクティスへ

### Z. 完了済み（旧プラン由来）

同じ調査を繰り返さないための記録。すべて参照ゼロ・依存ゼロを確認のうえ実施済み。

| ID | 項目 | 対象 | 状態 | 完了日 |
| --- | --- | --- | --- | --- |
| Z-1 | Karma 一式の削除 | `apps/web/karma.conf.js` / `src/test.ts` | ✅ | 2026-09-09 |
| Z-2 | TSLint の削除 | `apps/web/tslint.json` | ✅ | 2026-09-09 |
| Z-3 | 重複 tsconfig の削除 | `src/tsconfig{,.app,.spec}.json` | ✅ | 2026-09-09 |
| Z-4 | README「設定ファイルの役割」節 | `apps/web/README.md` | ✅ | 2026-09-09 |
| Z-5 | README「tsconfig の階層」節 | `apps/web/README.md` | ✅ | 2026-09-09 |
| Z-6 | README「削除済みの死骸」節 | `apps/web/README.md` | ✅ | 2026-09-09 |

### A. 実害があるもの（P0・目安 半日）

| ID | 項目 | 対象ファイル | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| A-1 | HttpClient 未提供の**再現確認** | － | － | ✅ | 2026-09-10 | `NullInjectorError` は出ない。Angular 22 の `HttpClient` は `providedIn: 'root'` |
| A-2 | `provideHttpClient(withInterceptorsFromDi())` 追加 **または** 提供元の特定と記録 | `src/app/app.config.ts` / `apps/web/README.md` | A-1 | ✅ | 2026-09-10 | 別の欠落が実在。`WebappInterceptor` が無効で `X-Clearml-Client` が落ちていた。追加して実測で復帰確認 |
| A-3 | `OnPush` を自作6コンポーネントへ追加 | `features/quality-pipeline/{containers,components}/**` | － | ✅ | 2026-09-10 | 6/6 完了 |
| A-4 | フォーム活性が入力に追従するか手動確認 | `components/start-run-form/` | A-3 | ✅ | 2026-09-10 | 追従した。`toSignal(form.statusChanges)` は元から在り、追加変更なし |
| A-5 | セレクタ重複の解消 `sm-root` → `sm-app-shell` | `src/app/app.component.ts` | － | ✅ | 2026-09-10 | クラス名は据え置き。参照は routes 経由のみで template 参照ゼロ |
| A-6 | 未使用 provider 配列の削除 + README 追記 | `business-logic/business-logic.providers.ts` | － | ✅ | 2026-09-10 | 生成物領域。再生成で復活しうる |
| A-7 | Phase A の検証 | － | A-2〜A-6 | ✅ | 2026-09-10 | `verify:web` 緑（227+3 tests）。`web:e2e` 18/18 緑 |

**完了条件**: A-7 が緑。かつ A-1 の結果（エラーの有無と原因）が記録されていること。

### B. ルールの機械化（P1・目安 半日）

| ID | 項目 | 対象ファイル | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| B-1 | `@ngrx/eslint-plugin` を flat config へ接続 | `apps/web/eslint.config.js` | － | ✅ | 2026-09-10 | `configs.all` で接続。337件検出。`prefer-effect-callback-in-block-statement` は理由を書いて `off` のまま |
| B-2 | `prefer-on-push-component-change-detection` を `error` 化 | `apps/web/eslint.config.js` | **A-3** | ✅ | 2026-09-10 | angular-eslint 22 でルールの意味が変化。咎めるのは `Default` への明示的離脱だけ（全体1件） |
| B-3 | baseline 更新（**自作分は baseline に入れず直す**） | `apps/web/eslint-baseline.json` | B-1, B-2 | ✅ | 2026-09-10 | errors 2861→3198 / warnings 56→1087。自作コードの違反は0件で更新 |
| B-4 | `use-track-by-function` 廃止 → `prefer-control-flow` を `error` 化 | `apps/web/eslint.config.js` | － | ✅ | 2026-09-10 | `prefer-control-flow` 違反0で `error` 化。`template/no-any` 86件、`prefer-self-closing-tags` 1032件（warn）を baseline へ |
| B-5 | `.gitignore` の責務分割（root / apps/web） | `.gitignore` / `apps/web/.gitignore` | － | ✅ | 2026-09-10 | `git check-ignore` で12対象の無視継続を確認。新規 untracked なし |
| B-6 | README の baseline 数値ドリフト解消 | `apps/web/README.md` | － | ✅ | 2026-09-10 | 数値を消し `totals` を見る旨だけ書いた |

**完了条件**: `pnpm run web:lint:baseline` が緑。`git status --porcelain apps/web` に生成物が出ない。

### C. 構造とドキュメント（P2・目安 1日）

| ID | 項目 | 対象ファイル | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| C-1 | `no-cross-feature-import` の description 修正 | `apps/web/web-boundaries.json` | － | ✅ | 2026-09-10 | 「shared へ置く。webapp-common は凍結領域」へ |
| C-2 | `no-shared-to-feature-import` の `from` に `src/app/shared/**` を追加 | `apps/web/web-boundaries.json` | － | ✅ | 2026-09-10 | 既存違反が1件あり（`header-menu.service` → `projects.consts`・ベンダー由来）`knownViolations` へ |
| C-3 | README「ディレクトリ構成」節 | `apps/web/README.md` | － | ✅ | 2026-09-10 | ファイル数は実測値に更新（features 143 / business-logic 607） |
| C-4 | README「feature 標準形」節 | `apps/web/README.md` | － | ✅ | 2026-09-10 | presentational の spec を書く決まりも明記 |
| C-5 | README「`.component` 接尾辞を維持」の決定記録 | `apps/web/README.md` | － | ✅ | 2026-09-10 | OnPush を明示する決定（A-3 の扱い）も同じ節に併記 |
| C-6 | presentational component の spec 追加（5件） | `features/quality-pipeline/components/**` | **A-3** | ✅ | 2026-09-10 | 5ファイル・43テスト追加（227→270）。5箇所の意図的な変異で全て検出を確認 |

**完了条件**: `pnpm run web:boundaries` / `pnpm run web:test` が緑。

### D. トリガー待ち（P3）

| ID | 項目 | 状態 | トリガー | 現状 |
| --- | --- | --- | --- | --- |
| D-1 | zoneless change detection（`stackup`） | ⏸ | ①自作が100% OnPush（A-3/B-2）②追従で `webapp-common` の OnPush 比率が上がる ③描画性能の問題が実測される | ①**達成済**（Angular 22 の既定が OnPush・離脱1件）／②実質達成／③観測なし。`report-widgets` は既に zoneless |
| D-2 | v20 命名規約（`.component` を落とす） | ⛔ | － | **採用しないと決定済み**。記録は C-5 |
| D-3 | `shared/` を自作共有層として育てる | ⏸ | 2つ目の自作 feature で共有したいものが自然に出たとき | 現在10ファイル。先回りして移さない |
| D-4 | `@defer` によるバンドル削減 | ⏸ | 初期表示の実測が問題になったとき | budget: initial warn 3.15MB / error 3.30MB |
| D-5 | `libs/` の新設 | ⏸ | ①2本目のデプロイ単位 ②`shared/` が自作100ファイル超 ③feature の所有権が分かれる | どれも未該当。**前提は D-3** |

---

## Stage 2 — 機械学習・MLOps の残り

**Stage 1（A〜C）完了により着手可能。**（W1〜W11・W13 は完了済み。記録は `x-old-plan/02_実装進捗.md`）

| ID | 項目 | 状態 | 依存 | 備考 |
| --- | --- | --- | --- | --- |
| W12 | CI強化とテスト拡充（**保留の解除**） | ✅ | Stage 1-C6 | 内訳 W12-1〜W12-10 すべて完了（10/10） |
| W14 | IaC と災害復旧 | ⬜ | － | Terraform 相当・backup/restore 演習 |
| W15 | チーム開発標準とドキュメント | ⬜ | Stage 1-C | README 整備がここへ直結。二重に書かない |
| W16 | コスト・容量・ライフサイクル管理 | ⬜ | － | W13 から HPO 費用可視化を引き継ぎ |
| M-1 | `ci.yml` が `services/tests` を走らせていない | ✅ | － | **W12-1 で解消**。単独の抜けではなく CI 再実装の症状だった |

### W12 の内訳

**着手順は 00 §5.1.13 に従う。** 1 が他の全ての前提。

| ID | 項目 | 依存 | 状態 | 完了日 | 備考 |
| --- | --- | --- | --- | --- | --- |
| W12-1 | `ci.yml` を npm script を呼ぶ形へ（M-1 と e2e の穴を同時に塞ぐ） | － | ✅ | 2026-09-11 | ジョブ分割は維持。**穴は3つあった**（`web:contract` も落ちていた）。一致を `ci:parity` ゲートで固定 |
| W12-2 | e2e を CI の別ジョブとして載せる | W12-1 | ✅ | 2026-09-11 | `verify` には足さない（手元は `pnpm web:e2e`）。**`ci:parity` の `COVERAGE` へ `web:e2e` → `e2e` ジョブを追加**し、verify の外にあっても CI から落ちたら失敗する形にした |
| W12-3 | test pyramid の定義（どの層で何を確かめるか） | W12-2 | ✅ | 2026-09-11 | 7層を `test-pyramid.json` へ宣言し `test:pyramid` で実態と突き合わせる。E2E だけ上限30件。ADR 005 |
| W12-4 | failure test（retry / timeout / API停止 / 破損Artifact） | W12-3 | ✅ | 2026-09-11 | 21件追加（ml 7 / services 3 / tools 11）。**4件の実害を発見して修正**：壊れた重みが `KeyError: 0` で落ちる箇所が2つ、Registry が読めないときに SDK の型のまま落ちる、seed の唯一の retry が無検査 |
| W12-5 | `prediction_api` の OpenAPI スナップショットと破壊変更検知 | W12-3 | ✅ | 2026-09-11 | consumer-provider にはしない（00 §5.1.4）。`openapi.json` をコミットし `services:test` が突き合わせる。**判定は完全一致**で、破壊/互換の分類は読み方だけを変える。新しい層もゲートも作らなかった。ADR 006 |
| W12-6 | smoke E2E（Dataset登録 → Queue学習 → 比較 → 昇格 → 推論） | W12-2 | ✅ | 2026-09-12 | 5件追加（E2E 18→23）。**1つのfixtureが1つの世界を返す**形にし、絞り込みも実際に当てる。最後の節目は推論そのものではなく受け渡し点（画面は推論APIを呼んでいない）。00 §5.1.8 |
| W12-7 | a11y 試験（axe） | W12-2 | ✅ | 2026-09-12 | 4件追加（E2E 23→27）。**対象は自作画面の中だけ**で0件固定。取り込んだ外枠（critical 2種・serious 1種）は凍結領域として対象外。00 §5.1.9 |
| W12-8 | 負荷試験（k6） | － | ✅ | 2026-09-12 | 期限は新しく決めず `alerts.yml` の値を借りる。ずれは gates のテストが見る（走らせる場所は増やさない）。**2026-09-12 に実走して緑**（10 VUs×30s・417req・失敗0・p95 808ms < 1000ms）。00 §5.1.10 |
| W12-9 | flaky test の検知・原因修正・再発防止の運用 | W12-2 | ✅ | 2026-09-12 | 隔離も retry もしない。`test:flaky` が**テスト1件ずつ**数える。運用と台帳は runbook 009。E2E 27件×4回で振れたものは無し。00 §5.1.11 |
| W12-10 | ADR 化（standalone / Signals・NgRx の責務 / lazy route / error handling） | － | ✅ | 2026-09-12 | ADR 007。**形は README、理由は ADR**で二重に書かない。失敗の扱いだけはどこにも無かった判断で、コードから起こした。00 §5.1.12 |

**M-1 は W12-1 に吸収した。** 単独の抜けではなく「CI が npm script を再実装している」
ことの症状だったため（00 §5.5）。

**着手前の実測（2026-09-10）**

| 層 | テスト数 | `verify`（手元） | `ci.yml` |
| --- | ---: | :---: | :---: |
| `ml/tests` | 813 | ✅ | ✅ |
| `tools/tests` | 69 | ✅ | ✅ |
| `services/tests` | 133 | ✅ | **❌** |
| `scripts/tests` | 2ファイル | ✅ | ✅ |
| `apps/web` unit | 273 | ✅ | ✅ |
| `e2e` (Playwright) | 18 | **❌** | **❌** |
| `web:contract` | 6 | ✅ | **❌** |

**W12-1 後（2026-09-11）**

| 層 | `verify`（手元） | `ci.yml` |
| --- | :---: | :---: |
| `ml/tests` / `tools/tests` / **`services/tests`** / `scripts/tests` | ✅ | ✅ |
| `apps/web` unit | ✅ | ✅ |
| **`web:contract`** | ✅ | ✅ |
| `e2e` (Playwright) | ❌ | ❌ → **W12-2** |
| `ci:parity`（新設） | ✅ | ✅ |

**W12-2 後（2026-09-11）**

| 層 | `verify`（手元） | `ci.yml` |
| --- | :---: | :---: |
| `ml/tests` / `tools/tests` / `services/tests` / `scripts/tests` | ✅ | ✅ |
| `apps/web` unit | ✅ | ✅ |
| `web:contract` | ✅ | ✅ |
| **`e2e` (Playwright)** | ❌（意図して。`pnpm web:e2e` で手で叩く） | ✅ **独立ジョブ** |
| `ci:parity` | ✅ | ✅ |

`e2e` の `verify` 列だけが ❌ のまま残る。速さのために入れていない（入れると
回されなくなる）。**ただし CI から落ちたときに気付けない状態にはしていない。**
`ci:parity` が `web:e2e` と `e2e` ジョブの対応を見ている。

**完了条件**: 上表の `ci.yml` 列が全て ✅ になり、W12-3〜W12-10 が閉じていること。
**2026-09-12 に達成**（W12-1〜W12-10 すべて ✅）。

### W14 の内訳

- [ ] network / compute / object storage / database / secret / monitoring のコード化
- [ ] dev / staging / production の分離
- [ ] MongoDB / Elasticsearch / file storage の backup / restore 演習
- [ ] RPO / RTO、容量計画、resource quota

### W15 の内訳

- [ ] C4図、データフロー、ML lifecycle、環境構成図
- [ ] ADR、coding standard、Definition of Done、review checklist
- [ ] ownership（CODEOWNERS 相当）、branch / release / versioning 規約
- [ ] onboarding、運用 runbook、障害訓練、postmortem
- [ ] model card、dataset card、評価上の制約・公平性・利用禁止条件

### W16 の内訳

- [ ] project / team / Task 単位の cost attribution
- [ ] Queue 優先度、並列数、CPU/GPU quota、停止ルール
- [ ] Dataset / Model / Artifact / log の retention と archive / delete policy
- [ ] HPO の budget 上限、idle Agent 停止、容量アラート

**Stage 2 の完了条件**

- [ ] staging 相当環境で主要業務フローを自動検証できる
- [ ] API 破壊変更・性能劣化・重大な a11y 違反をリリース前に検知できる
- [ ] IaC から環境を再作成でき、backup から復旧できる
- [ ] 新規参画者が資料だけで環境構築し、変更箇所と担当境界を判断できる
- [ ] 実験1回と環境単位の概算費用を可視化でき、上限超過前に停止・通知できる

---

## Stage 3 — 既存 ClearML コードの読解

**Stage 1 / 2 の順番待ちにしない。** 実装の合間に1項目ずつ回す。

`Agent実装` は実装手段の記録でありLevel判定には含めない。不要な場合は `-`。

| ID | 学習対象 | Agent実装 | コード読解 | 自力説明 | 自力変更 | テスト | Level | 状態 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | ---: | --- |
| P0-0 | 学習ベースラインと安全網 | - | [x] | [x] | [ ] | [x] | 2 | 🔄 |
| P0-1 | Task一覧・詳細 | - | [ ] | [ ] | [ ] | [ ] | 0 | ⬜ |
| P0-2 | Queue / Agent | [ ] | [ ] | [ ] | [ ] | [ ] | 0 | ⬜ |
| P0-3 | Pipeline | [ ] | [ ] | [ ] | [ ] | [ ] | 0 | ⬜ |
| P0-4 | Evaluation / Model Registry | [ ] | [ ] | [ ] | [ ] | [ ] | 0 | ⬜ |
| P0-5 | Serving | [ ] | [ ] | [ ] | [ ] | [ ] | 0 | ⬜ |

**Level**: 1=読める / 2=説明できる / 3=壊さず変更できる。**学習済みは Level 3。**

### 次の一手

| ID | 次にやること |
| --- | --- |
| P0-0 | ベースラインは実測済（2026-09-12）。安全網は型とビルドまで。**Level 3 は凍結領域へ小変更を入れる判断が出てから**。いまは 2 で置く |
| P0-1 | route から API、state、UI までを1本 trace する |
| P0-2 | Queue 投入から Task status 表示までを追う |
| P0-3 | controller、step、失敗経路を追う |
| P0-4 | model から Task、Dataset、評価へ追跡する |
| P0-5 | Angular request から model version 付き response までを追う |

### Level 3 へ上げる前の最終確認

- [ ] ユースケースと代表的な失敗条件を説明できる
- [ ] 既存の処理経路を端から端まで trace した
- [ ] state、Effect、API境界の所有者と設計理由を説明できる
- [ ] Agent が作った diff を全て読んだ
- [ ] 自分で仕様の異なる小変更を完了した
- [ ] 正常系と代表的な失敗系のテストを追加した
- [ ] テストを意図的に壊して検出力を確認した
- [ ] loading / empty / success / failure / cancel の該当 state を確認した
- [ ] subscription、polling、request cancel の寿命を説明できる
- [ ] API model から画面表示までの変換を説明できる
- [ ] 変更の影響範囲と残るリスクを説明できる

---

## 実施順序

```text
Stage 1
  A-1 ─→ A-2                              ┐
  A-3 ─→ A-4                              │
  A-5                                     ├─→ A-7（検証）
  A-6                                     ┘
        │
        ├─→ B-1 ─┐
        │   B-4  ├─→ B-3（baseline 更新）
  A-3 ──┴─→ B-2 ─┘
            B-5 / B-6（独立）
        │
        ├─→ C-1 / C-2（boundaries）
        ├─→ C-3 / C-4 / C-5（README）
  A-3 ──┴─→ C-6（presentational spec）
        ↓
Stage 2
  C-6 ─→ W12（保留解除）
           W12-1（ci.yml を script 呼び出しへ）
             ↓
           W12-2（e2e を CI へ）────────┬─→ W12-6（smoke E2E）
             ↓                          ├─→ W12-7（a11y）
           W12-3（test pyramid）──┬─────┴─→ W12-9（flaky 運用）
                                  ├─→ W12-4（failure test）
                                  └─→ W12-5（OpenAPI 破壊変更検知）
           W12-8（負荷）/ W12-10（ADR）は独立
         → W14 → W16
  C-3/C-4 ─→ W15
        ↓
Stage 3（Stage 1・2 と並行）
  P0-0 → P0-1 → P0-2 → P0-3 → P0-4 → P0-5
```

**A-3（OnPush）が B-2 と C-6 の両方の前提。** ここを最初に通すこと。（Stage 1 完了済み）

**W12-1 が Stage 2 の全ての前提。** CI が本当に全部走る状態にしてから
テストを足す。走らない場所へ足しても緑になるだけである（00 §5.1.13）。

---

## 検証コマンド

```bash
pnpm run verify:web              # 各 Stage はこれが緑になることで閉じる

pnpm run web:gates:test          # ゲートスクリプト自体のテスト
pnpm run ci:parity               # ci.yml が verify を本当に走らせているか
pnpm run test:pyramid            # テストが決めた層に置かれているか
pnpm run web:lint:baseline       # ESLint 違反が baseline を超えていないか
pnpm run web:boundaries          # レイヤ違反（feature間 / 生成API）
pnpm run web:contract            # pipeline contract
pnpm run web:typecheck:strict    # strict 対象範囲の型検査
pnpm run web:test                # Vitest（stackup + report-widgets）
pnpm run web:e2e:typecheck
pnpm run web:build
pnpm run web:scan:build
pnpm run web:e2e                 # Playwright
```

---

## 更新ルール

1. **この表では状態・完了日・実測値だけを更新する。** 方針が変わったら `00_プラン.md`
   を直し、ここには結果だけを反映する
2. 完了にするのは**検証が緑になってから**。
   「変更した」ではなく「検証が通った」が完了の定義
3. 途中で分かったこと（A-1 の再現結果など）は**備考欄に一行**。
   詳細は `00_プラン.md` か `apps/web/README.md` へ。**この表を厚くしない**
4. サマリの件数は、表を更新したら合わせて直す
5. `x-old-plan/` は更新しない。履歴として凍結する

---

## 作業証跡（1作業ごとに追記）

```text
日付:
対象ID:
今回の範囲:
対象外:
完了条件:

やったこと:
読んだ主要ファイル:
確認した処理経路:

自分で加えた変更:
追加したテスト:
意図的に壊したテストと確認結果:
実行した確認コマンドと結果:

説明できるようになったこと:
まだ説明できないこと:
次の一手:
```

### 2026-09-10 — Stage 1 / A（A-1〜A-7）

```text
日付: 2026-09-10
対象ID: A-1, A-2, A-3, A-4, A-5, A-6, A-7
今回の範囲: Stage 1-A（実害があるもの）を全件
対象外: B / C / D。webapp-common などベンダー由来コード
完了条件: A-7（verify:web と web:e2e が緑）かつ A-1 の結果が記録されていること

やったこと:
  A-1 プランの前提（NullInjectorError が出る）を Playwright で再現確認 → 出なかった
      Angular 22 で HttpClient / HttpHandler / HttpBackend が providedIn:'root' に
      なっていたため。既定 backend は FetchBackend
  A-1 提供元を特定する過程で別の欠落を発見。HttpInterceptorHandler は
      HTTP_INTERCEPTOR_FNS からしか chain を組まず、HTTP_INTERCEPTORS を
      橋渡しするのは withInterceptorsFromDi() だけ。よって WebappInterceptor が
      一度も動いていなかった。実測でも X-Clearml-Client ヘッダが付いていなかった
  A-2 app.config.ts に provideHttpClient(withInterceptorsFromDi()) を追加。
      同じ Playwright 手順でヘッダが付くようになったことを確認。README に記録
  A-3 quality-pipeline の6コンポーネントへ OnPush を追加
  A-4 フォームの活性・aria-invalid・ヒントの強調が入力に追従することを確認
  A-5 app.component.ts の selector を sm-root → sm-app-shell へ
  A-6 business-logic.providers.ts を削除。README「削除済みの死骸」へ理由を追記
  A-7 verify:web / web:e2e を実行

読んだ主要ファイル:
  apps/web/src/app/app.config.ts
  apps/web/src/app/core/core.providers.ts / core/app-init.ts / main.ts
  apps/web/src/app/build-specifics/index.ts
  apps/web/src/app/webapp-common/core/interceptors/webapp-interceptor.ts
  apps/web/src/app/webapp-common/shared/services/login.service.ts / configuration.service.ts
  apps/web/src/app/features/quality-pipeline/**（6コンポーネント + template）
  node_modules/@angular/common/fesm2022/_module-chunk.mjs（HttpClient の DI 定義）

確認した処理経路:
  main.ts → bootstrapApplication → provideAppInitializer → loadUserAndPreferences
    → ConfigurationService / BaseLoginService（どちらも inject(HttpClient)）
    → HttpClient → HttpHandler → HttpInterceptorHandler.handle
    → HTTP_INTERCEPTOR_FNS から chain 構築 → FetchBackend
  ここで HTTP_INTERCEPTORS が chain に入らないことが欠落の実体だった

自分で加えた変更:
  app.config.ts: provideHttpClient(withInterceptorsFromDi()) 追加 + 理由コメント
  quality-pipeline の6コンポーネント: ChangeDetectionStrategy.OnPush 追加
  app.component.ts: selector を sm-app-shell へ（クラス名は据え置き）
  business-logic/business-logic.providers.ts: 削除
  apps/web/README.md: 「HttpClient と interceptor の提供」節を新設、
                      「削除済みの死骸」表へ A-6 の行を追加

追加したテスト:
  なし。A-1 / A-4 の確認は使い捨ての Playwright spec で行い、確認後に削除した
  （常設のテストにするなら C-6 と一緒に置く）

意図的に壊したテストと確認結果:
  A-2 の修正前後で同じ spec を流し、X-Clearml-Client=(none) → Webapp-2.3.0-000 に
  変わることを確認した。修正を戻せば (none) に戻る＝検出力があることの確認になっている
  なお dev server を再起動せずに測ると古い bundle が返り、修正後も (none) に見えた。
  この手順で測るときは ng serve を落としてから流すこと

実行した確認コマンドと結果:
  pnpm run verify:web  → 緑
    web:gates:test / web:lint:baseline / web:boundaries（既知216件・新規なし）
    web:contract / web:typecheck:strict（範囲内エラーなし）
    web:test（stackup 227 tests / report-widgets 3 tests・全 pass）
    web:e2e:typecheck / web:build / web:scan:build（資格情報の混入なし）
  pnpm run web:e2e     → 18/18 pass

説明できるようになったこと:
  Angular 22 で provideHttpClient() が「HttpClient を使えるようにするもの」ではなく
  「interceptor chain と backend を構成するもの」に役割が移っていること
  クラス型 interceptor（HTTP_INTERCEPTORS）と関数型 interceptor の橋渡しの仕組み
  signal と Reactive Forms を混ぜた component が OnPush でも追従する理由
  （テンプレート上のイベントリスナが宣言元 view を dirty にする）

まだ説明できないこと:
  webapp-common 側で HttpClient を使う経路のうち、401 → logout が実際に
  どの画面遷移になるか（今回 interceptor が復帰したので、次に 401 が起きたときに追う）
  XSRF interceptor が有効化されたことによる ClearML server 側の期待値

次の一手:
  B-1（@ngrx/eslint-plugin の接続）→ B-2（OnPush ルールの error 化）
  A-3 が済んだので B-2 の前提は揃っている
```

### 2026-09-10 — Stage 1 / B（B-1〜B-6）

```text
日付: 2026-09-10
対象ID: B-1, B-2, B-3, B-4, B-5, B-6
今回の範囲: Stage 1-B（ルールの機械化）を全件
対象外: C / D。ベンダーコードの違反解消（baseline に載せるだけ）
完了条件: pnpm run web:lint:baseline が緑。git status --porcelain apps/web に生成物が出ない

やったこと:
  B-1 @ngrx/eslint-plugin を configs.all で接続。31ルールが有効になり337件を検出
      prefer-effect-callback-in-block-statement（264件）は off のまま残した。
      整形するだけで挙動が変わらず、自動修正の出力も読みにくいため。
      以前は同じ1行がプラグイン未登録で無効だったが、いまは意図した設定になった
  B-2 prefer-on-push-component-change-detection を error 化。
      ただし angular-eslint 22 でルールの意味が変わっていた（下記）
  B-4 use-track-by-function を廃止し prefer-control-flow を error 化（違反0）。
      template/no-any を error（86件）、prefer-self-closing-tags を warn（1032件）
  B-3 自作コードの違反が0件であることを確認してから baseline を更新
      errors 2861 → 3198 / warnings 56 → 1087
  B-5 .gitignore を責務で分割。root は横断の関心事、apps/web はアプリ固有
  B-6 README から baseline の数値を削除し、eslint-baseline.json を見る旨だけ残した

読んだ主要ファイル:
  apps/web/eslint.config.js / eslint-baseline.json
  scripts/eslint-baseline.mjs（件数の数え方と regression 判定）
  node_modules/@ngrx/eslint-plugin/src/index.js（configs の形）
  node_modules/@angular-eslint/eslint-plugin/dist/rules/
    prefer-on-push-component-change-detection.js
  node_modules/@angular/core/types/_debug_node-chunk.d.ts（ChangeDetectionStrategy）
  node_modules/@angular/core/fesm2022/_debug_node-chunk.mjs（ɵɵdefineComponent）

確認した処理経路:
  eslint-baseline.mjs は eslint . --format json を回して totals と byRule を取る。
  totals が増えたら失敗し、増えた rule 名を出す。自作分を直さずに update すると
  そのまま固定されるので、update の前に自作コードの違反0を確認する手順を踏んだ

自分で加えた変更:
  eslint.config.js: ngrx プラグイン接続 / OnPush ルール error 化 /
                    template ルール3件の入れ替え / off の理由コメント
  eslint-baseline.json: 更新
  src/app/app.ts: <router-outlet></router-outlet> → <router-outlet />（自動修正）
  .gitignore / apps/web/.gitignore: 責務で分割
  apps/web/README.md: baseline 数値の削除
  x-plan-改訂/00_プラン.md: §2.1 / §4.2 / §4.3 A-3 / §4.4 B-1 / §4.4 B-2 / §7 D-1 へ注記

追加したテスト:
  なし（設定変更のため）

意図的に壊したテストと確認結果:
  baseline 更新前に web:lint:baseline を回し、errors 3198/2861 で失敗することを確認した。
  ゲートが増加を検出することの確認になっている

実行した確認コマンドと結果:
  pnpm run verify:web → 緑
    ESLint: errors 3198/3198, warnings 1087/1087
    境界の検査: 新しい違反なし（既知216件）
    strict の検査: 移行済みの範囲にエラーなし
    web:test 227 + 3 tests 全 pass / web:build / web:scan:build
  git check-ignore で build / .angular / coverage / reports / playwright-report /
    test-results / .env / credentials.json / node_modules / clearml.conf /
    .coverage / AGENTS.local.md の12対象が引き続き無視されることを確認
  git status --porcelain --untracked-files=all → 新規 untracked なし

説明できるようになったこと:
  Angular 22 では OnPush が既定の変更検知戦略であること。
  changeDetection を省略すると OnPush になり、離脱には Default(=Eager) の明示が要る
  （ɵɵdefineComponent の onPush: changeDetection !== Eager）。
  実測で @Component 345個のうち明示的な離脱は1個だけだった
  したがって prefer-on-push-component-change-detection は
  「OnPush を付け忘れた component」ではなく「意図的に Default へ落とした component」を
  検出するルールになっている
  eslint-baseline が総数と rule 別の両方で増加を見ていること

まだ説明できないこと:
  @ngrx の337件のうち、avoid-dispatching-multiple-actions-sequentially（144件）が
  どこまで実害のあるパターンなのか。ベンダーコードなので今は baseline に置いた
  prefer-self-closing-tags 1032件を warn で抱えることが、将来 warn チャネルを
  使いたくなったときにどれだけ邪魔になるか

次の一手:
  A-3 の宣言を残すか外すかを決める（実行時 no-op と分かったため）
  決めたうえで C-1 / C-2（web-boundaries.json）へ
```

### 2026-09-10 — Stage 1 / C（C-1〜C-6）

```text
日付: 2026-09-10
対象ID: C-1, C-2, C-3, C-4, C-5, C-6
今回の範囲: Stage 1-C（構造とドキュメント）を全件。これで Stage 1 の A〜C が閉じる
対象外: D（トリガー待ち）。ベンダーコードの構造変更
完了条件: pnpm run web:boundaries / pnpm run web:test が緑

やったこと:
  C-1 no-cross-feature-import の description を「shared へ置く」へ。
      「webapp-common へ置く」は §4.1 の凍結方針と正反対だった
  C-2 no-shared-to-feature-import の from に src/app/shared/** を追加。
      「shared はまだ綺麗」という前提が外れ、既存違反が1件あった
      （header-menu.service.ts -> ~/features/projects/projects.consts、ベンダー由来）
      PROJECT_ROUTES を動かすとベンダーコードに波及するため knownViolations へ入れた。
      ゲート自体は立ったので、以後の新規違反は止まる
  C-3 README「ディレクトリ構成」節。ファイル数はプラン記載値ではなく実測値を使った
      （features 138→143 / business-logic 608→607）
  C-4 README「feature の標準形」節
  C-5 README「変更検知とコンポーネントの命名（決定の記録）」節。
      .component 接尾辞の維持に加えて、OnPush を明示し続ける決定も同じ節へ書いた
  C-6 presentational component の spec を5件追加（43テスト）

読んだ主要ファイル:
  apps/web/web-boundaries.json / scripts/web-boundaries.mjs
  apps/web/src/app/shared/services/header-menu.service.ts
  apps/web/src/app/features/projects/projects.consts.ts
  quality-pipeline の components 5件（.ts と .html）
  quality-pipeline-page.component.spec.ts（既存の書き方に合わせるため）

確認した処理経路:
  web-boundaries.mjs は rules の from / deny / allow で import を判定し、
  knownViolations に無いものだけを「新しい違反」として失敗させる。
  from に足すとその配下が新たに検査対象になるので、既存違反が表に出る

自分で加えた変更:
  web-boundaries.json: description 2件の修正 / from への追加 / knownViolations 1件
  apps/web/README.md: 「ディレクトリ構成」「feature の標準形」
                      「変更検知とコンポーネントの命名（決定の記録）」の3節を新設
  5件の spec を新規作成:
    pipeline-steps-table   6テスト  打ち切りの注記 / 空 / 順序 / UTC / data-status
    evaluation-scores-table 6テスト 桁揃え / 0埋め / null は — / 空
    run-summary            8テスト  UTC / 理由の有無 / 停止の可否 / output の発火
    production-model-card  5テスト  null で文が出る / dl が出ない / 各facts
    start-run-form        18テスト  押せる条件 / Enter からの送信 / a11y / 出自の説明

追加したテスト:
  43件（stackup 227 → 270 / Test Files 55 → 60）

意図的に壊したテストと確認結果:
  5箇所を同時に変異させ、いずれも検出されることを確認した（8件が失敗）。
    1. @if (truncated()) → @if (true)
       → 「says nothing about a limit when the whole run is on screen」が失敗
    2. scoreFormat '1.3-3' → '1.1-5'
       → 「rounds every metric to three decimals」「pads a short value out to
          three decimals」が失敗（既存の container spec も失敗した）
    3. run-summary の timeZone 'UTC' → 'America/New_York'
       → 「shows the times in UTC, and says which zone that is」が失敗
    4. 「No model has been promoted to production yet.」の文言を削除
       → 「says so in a sentence rather than leaving the card blank」が失敗
    5. submit() の canSubmit() ガードを削除
       → 「stays silent when submitted from the keyboard while it cannot start」が失敗
  変異はすべて戻し、git status で復元を確認した

実行した確認コマンドと結果:
  pnpm run verify:web → 緑
    ESLint: errors 3198/3198, warnings 1087/1087
    境界の検査: 新しい違反なし（既知 217 件・C-2 で 216 から1件増）
    strict の検査: 移行済みの範囲にエラーなし
    web:test 270 + 3 tests 全 pass
  pnpm run web:e2e → 18/18 pass

説明できるようになったこと:
  web-boundaries の from / deny / allow / knownViolations の関係と、
  from を広げると既存違反が表に出るという性質
  presentational component を fixture.componentRef.setInput() で単体テストする形
  この feature の presentational が持っている判断が何であるか
  （桁を揃える / 打ち切りを黙らない / 「まだ無い」を空白ではなく文で出す /
    押せることと起動してよいことを一致させる）

まだ説明できないこと:
  header-menu.service.ts の PROJECT_ROUTES 依存を、ベンダー追従を壊さずに
  解消する方法があるかどうか
  container spec と presentational spec で重複しているテスト
  （桁揃えと UTC）を、どちらに寄せるべきか

次の一手:
  Stage 1 の A〜C はこれで閉じた。D はトリガー待ちで着手しない。
  Stage 2 の W12（C-6 完了により保留解除）へ進むか、
  Stage 3 の P0-0 を並行で回すかを選ぶ
```

### 2026-09-11 — Stage 2 / W12-1（ci.yml を script 呼び出しへ）

```text
日付: 2026-09-11
対象ID: W12-1（M-1 を吸収）
今回の範囲: .github/workflows/ci.yml を package.json の script を呼ぶ形へ変える。
            一致を検査するゲートを新設する。
対象外: W12-2（e2e を別ジョブとして載せる）。次のセッションで行う。
完了条件: pnpm run verify:web が緑。ci:parity が通る。
          services/tests と web:contract が ci.yml で走ること。

やったこと:
  着手前の実測で、穴は2つではなく3つだった。
    1. services/tests 133件（認可13件を含む）が CI で走っていない（M-1）
    2. e2e が手元でも CI でも自動では走らない
    3. web:contract が CI に無い ← 着手時に発見。00 §5.1.4 で
       「担当済み」としていた層が、CI では担当していなかった

  数え直しではなく、数え間違いが起きない形にした。
  scripts/ci-parity.mjs を新設し、verify:python / verify:web / verify:security の
  各要素が ci.yml の担当ジョブで同じ順序で呼ばれていることを検査する。
  ゲートは片方向だけ見る。「verify にあって CI に無い」を失敗とし、
  CI 固有の手順（lock 検証・Trivy 走査）には何も言わない。

  ci.yml の各ジョブを script 呼び出しへ置き換えた。
  ジョブ分割（python / web / security / supply-chain）は維持した。

読んだ主要ファイル:
  .github/workflows/ci.yml
  package.json（verify / verify:python / verify:web / verify:security の連鎖）
  scripts/pipeline-contract.mjs、scripts/tests/pipeline-contract.test.mjs（書き方を揃えるため）
  apps/web/package.json、apps/web/playwright.config.ts（W12-2 の下調べ）

確認した処理経路:
  package.json の verify:* → 末端 script → 実コマンド
  ci.yml の jobs.<job>.steps[].run に書かれた pnpm run <script>
  この2つが一致しているかを ci:parity が突き合わせる

自分で加えた変更:
  新設 scripts/ci-parity.mjs
    expand()      pnpm run の連結だけで構成された script を末端まで展開する。
                  実コマンドを含む script（py:coverage など）はそこで止める。
                  「どこまで展開するか」の別表を持たずに済む規則
    ciJobs()      ci.yml を YAML として読まず行で拾う。見たいのは run: に
                  書かれた文字列そのもので構造ではないため。依存を増やさない
    missing()     順序を保った包含関係。「走っていない」と「順序が違う」を
                  区別して報告する（直し方が違う）
  package.json
    ci:parity を追加し、verify:web の web:gates:test の直後に入れた。
    検査対象は web に限らないが、「先に自分を疑う」段に属するため
  ci.yml
    python job    Node + pnpm を足し、py:lock / py:lint / py:typecheck /
                  py:coverage を呼ぶ形へ。lock 検証も手で書き写した4行から
                  pnpm run py:lock へ寄せた。pnpm install は不要（node の
                  依存を使わない）
    web job       ci:parity と web:contract のステップを追加。
                  生成物の走査を pnpm run web:scan:build へ寄せ、
                  そのために .venv（pyyaml のみ）を作る形にした
    security job  pnpm run sec:scan を呼ぶ形へ。同じく .venv が要る
    supply-chain  変更なし（手元の verify に対応物が無い CI 固有のジョブ）
  README.md
    「CIがこの検証を本当に走らせていること」節を追加。
    ゲート表に ci:parity を追加。verify:web の説明を実態へ合わせた

追加したテスト:
  scripts/tests/ci-parity.test.mjs 12件
    ジョブごとに script を出現順で拾う
    jobs: の外の行を拾わない（permissions: をジョブと読み違えない）
    註釈の中の例をコマンドと読み違えない
    pnpm run の連結だけの script を末端まで展開する
    実コマンドを含む script はそこで止める
    存在しない script / 自分自身を呼ぶ script で黙らずに落ちる
    CI 固有の手順が混ざっていても何も言わない
    verify にあって CI に無い script を報告する
    順序の入れ替わりを、走っていないのとは別の言い方で報告する
    担当ジョブが消えていたら報告する
    どのジョブの何が落ちているかまで含めて報告する

意図的に壊したテストと確認結果:
  ci.yml から「Pipeline contract」ステップを一時的に削除
    → ci:parity が exit 1、「web job: web:contract が走っていない
      （verify:web に入っている）」と報告した。復元して再度緑を確認
  着手前の ci.yml に対して ci:parity を走らせたところ 7 件検出した
    （py:lint / py:typecheck / py:coverage / ci:parity / web:contract /
      web:scan:build / sec:scan）。ゲートが後追いで書かれたものでないことの確認

実行した確認コマンドと結果:
  node --test 'scripts/tests/**/*.test.mjs' → 31 pass（既存19 + 新規12）
  pnpm run ci:parity → 「CI の一致: verify の 14 件すべてが ci.yml で走る」
  pnpm run web:contract → 6 件すべて一致
  pnpm run web:boundaries → 新しい違反なし（既知 217 件）
  pnpm run web:lint:baseline → errors 3198/3198, warnings 1087/1087
  python3 -c "yaml.safe_load(...)" → ci.yml は妥当な YAML。jobs は4つのまま
  pnpm run verify:web → 緑

説明できるようになったこと:
  「CI が赤くならない種類の欠落」は、足りない行を足しても再発すること。
  止められるのは、欠落そのものではなく欠落を検知する仕組みを置いたときだけ
  ジョブ分割とドリフト検知が両立すること。分割を保つ限り CI 側は
  script を列挙し続けるので、列挙が正しいことを別のゲートで見る
  ゲートを片方向にする理由。完全一致にすると CI 固有の手順の置き場所が無くなる

まだ説明できないこと:
  README.md 84行目付近の「ESLint違反は2,871件（warning 56件）、禁止依存は216件」
  が実測（3198 / 1087 / 217）とずれている。B-6 のドリフト修正から漏れた箇所と
  思われるが、W12-1 の範囲外なので触っていない
  python job に Node を入れたことで、Node の準備失敗が Python の結果を
  隠すようになった。分割の理由（片方の失敗でもう片方を隠さない）と
  どこまで両立しているか

次の一手:
  W12-2。e2e を CI の別ジョブとして載せる。
  playwright install --with-deps chromium と、CI=1 のときの webServer 起動
  （reuseExistingServer が false になる）が追加で要る。
  verify:web には足さない。手元は apps/web の verify:test を使う
```

### 2026-09-11 — Stage 2 / W12-2（e2e を CI の別ジョブへ）

```text
日付: 2026-09-11
対象ID: W12-2
今回の範囲: Playwright 18件を ci.yml の独立ジョブとして走らせる。
            verify の外にあっても CI から落ちたら失敗する形にする。
対象外: W12-6（smoke E2E）、W12-7（a11y）。載せる土台だけを作る。
完了条件: CI=1（= CI と同じ経路）で 18件が緑。pnpm run verify:web が緑。
          e2e ジョブから web:e2e を消すと ci:parity が落ちること。

やったこと:
  着手前に「CI で本当に足りるのか」を先に確かめた。確かめたのは3点。
    1. backend が要るか  → 要らない。e2e/fixtures の page.route が
       **/service/** と api.github.com を全て差し替えている
    2. clearml.conf が無くても start が通るか → 通る。
       apps/web/scripts/generate-credentials.mjs は資格情報が見つからないとき
       警告を出して空の credentials.json を書き、exit 0 で続ける。
       e2e は認証済みの画面を前提にしていないので、これで足りる
    3. dev server の起動時間 → 手元の cold start で 14 秒

  そのうえで CI=1 を付けて手元で流した（reuseExistingServer が false になり、
  Playwright が corepack pnpm start を自前で起動する経路）。18件すべて緑。
  WebServer のログに 127.0.0.1:8008 への proxy error が出るが、これは
  vite の proxy が backend を探しに行くもので、テストの経路とは別である。

読んだ主要ファイル:
  apps/web/playwright.config.ts（webServer / forbidOnly / reporter）
  apps/web/e2e/fixtures/clearml-api.fixture.ts、quality-pipeline.fixture.ts
  apps/web/scripts/generate-credentials.mjs（CI で何が起きるか）
  scripts/ci-parity.mjs、scripts/tests/ci-parity.test.mjs
  scripts/clearml-run.sh（root の web:* が通る経路。web:e2e は通らない）

確認した処理経路:
  pnpm run web:e2e → pnpm --dir apps/web e2e → playwright test
    → webServer: corepack pnpm start → config:credentials → ng serve
    → page.route がブラウザ内で API 応答を差し替える

自分で加えた変更:
  ci.yml
    e2e job  checkout / setup-node / corepack / pnpm store cache /
             pnpm install --frozen-lockfile /
             pnpm --dir apps/web e2e:install（ブラウザとOSライブラリ）/
             pnpm run web:e2e。失敗時のみ playwright-report・reports/e2e・
             test-results を artifact へ上げる（trace は retain-on-failure なので
             成功時は残らない）。.venv は要らない（web:e2e は python を使わない）
  scripts/ci-parity.mjs
    COVERAGE に {chain: 'web:e2e', job: 'e2e'} を追加した。
    COVERAGE は verify の連鎖だけの表ではなくなった。単体の script も
    対応先のジョブごと書ける。これが今回の本題である（下記「説明できるように
    なったこと」）
    differences() が、単体で書いた項目については出所を添えないようにした。
    「web:e2e が走っていない（web:e2e に入っている）」は読み手に何も足さない
  apps/web/playwright.config.ts
    webServer.timeout を CI のとき 180 秒へ（手元は 120 秒のまま）。
    CI には .angular のキャッシュが無く初回ビルドが手元より遅い。
    足りないと「E2E が失敗した」と読める形で「まだ立ち上がっていない」が出る
  README.md
    「E2E」節を追加。verify に入れない理由、backend が要らないこと、
    reuseExistingServer の落とし穴（古い dev server を使い回す）を書いた
    ゲート表の直後の件数を実測へ直した（2,871/56/216 → 3,198/1,087/217）。
    W12-1 で「範囲外」として残していた箇所

追加したテスト:
  scripts/tests/ci-parity.test.mjs 1件
    単体で書いた項目は、出所を二度言わずに報告する

意図的に壊したテストと確認結果:
  e2e ジョブから「- name: E2E / run: pnpm run web:e2e」を一時的に削除
    → ci:parity が exit 1、「e2e job: web:e2e が走っていない」と報告した。
      復元して再度緑を確認。ジョブを足しただけでは誰も守っていないので、
      これが今回の実質的な成果である

実行した確認コマンドと結果:
  CI=1 pnpm run web:e2e → 18 passed（35.3s）。dev server は Playwright が起動
  cold start の実測 → 14 秒（手元・8コア）
  node --test 'scripts/tests/**/*.test.mjs' → 32 pass（既存31 + 新規1）
  pnpm run ci:parity → 「CI の一致: 手元の検証 15 件すべてが ci.yml で走る」
  python3 -c "yaml.safe_load(...)" → 妥当な YAML。jobs は5つ
    （python / web / e2e / security / supply-chain）
  pnpm run verify:web → 緑

説明できるようになったこと:
  「verify の外に出す」と「CI から落ちたら失敗する」は別の話であること。
  速さのために verify から外すのは正しいが、外した瞬間に W12-1 で塞いだ穴が
  そこだけ再発する。ゲートの表を verify の連鎖に限らない形へ広げれば両立する
  CI で e2e が動く条件を、走らせる前に列挙できること。backend / 資格情報 /
  起動時間の3点。どれも「落ちてから調べる」とログが読みにくい種類の失敗である

まだ説明できないこと:
  webServer.timeout 180 秒が GitHub の runner で足りるか。手元の 14 秒からの
  見積もりでしかない。初回の CI 実行で実測して決める
  proxy error（127.0.0.1:8008）がログに残り続けること。テストは通るが、
  失敗時のログを読むときに邪魔になる。proxy を e2e のときだけ切れるか
  e2e ジョブが web ジョブと install を二重に走らせていること。
  分けた理由（結果が隠れない）と、実行時間の重複のどちらを採るか

次の一手:
  W12-3。test pyramid の定義（どの層で何を確かめるか）。
  実測値は 00 §5.1.1 の表。W12-2 までで「全部走る」状態になったので、
  次は「どの層に何を置くか」を決める段になる
```

### 2026-09-11 — Stage 2 / W12-3（test pyramid の定義）

```text
日付: 2026-09-11
対象ID: W12-3
今回の範囲: どの層で何を確かめるかを決め、宣言と実態のずれをゲートで見る形にする。
対象外: W12-4 以降で足す個別のテスト。層の中身は増やさない。
完了条件: pnpm run verify:web が緑。決めた層の外へテストを置くと落ちること。

やったこと:
  7層を test-pyramid.json へ宣言した。層ごとに「確かめること」と
  「確かめないこと」を両方書いた。片方だけだと、境界が重なっても気付けない。
    ゲート自身 / ml 単体 / services 単体 / tools 単体 /
    Angular 単体（自作）/ Angular 単体（取り込み）/ E2E

  規則は1つに絞った。**下の層で確かめられることを、上の層で確かめない。**
  決め手は「同じ判断を2か所に書くと、遅いほうだけが残る」こと。
  桁の書式を unit と E2E の両方で確かめておくと、書式を変えたときに
  どちらかを直せば緑になり、消えるのは速いほうである。

  宣言だけでは W12-1 と同じことが起きるので、scripts/test-pyramid.mjs で
  実態と突き合わせた。見るのは3つ。
    1. すべてのテストがいずれか1つの層に属すること（未分類・二重所属を失敗）
    2. 層が宣言した script が package.json に在ること
    3. 上限を宣言した層が超えていないこと（いまは E2E の30件だけ）

  上限を E2E にだけ置いたのは、ピラミッドが崩れるのは上が太るときだけで、
  それは件数に出るためである。下の層は増えて困らない。

  件数は静的に数えた（Python は def test_、TS/mjs は it( test( の行）。
  実行して数えると全層を走らせることになり、verify の先頭で軽く回せなくなる。
  数え方が実測と一致することは着手時に確かめた（813/133/69/273/18/32）。

読んだ主要ファイル:
  package.json（verify:web の連鎖）、.github/workflows/ci.yml
  scripts/ci-parity.mjs（ゲートの書き方を揃えるため）
  scripts/web-boundaries.mjs / scripts/web-strict.mjs（glob の解釈が2通りあった）
  apps/web/stryker.execution.config.json（層ではないものの位置づけを決めるため）
  apps/web/vitest.config.ts、apps/web/package.json

確認した処理経路:
  test-pyramid.mjs → リポジトリ全体を走査（node_modules / .venv などは除く）
    → ファイル名の patterns で拾う → layers の include / exclude で分類
    → 層ごとに静的にケース数を数える → 未分類・二重所属・script 不在・
      空の層・上限超過を報告

自分で加えた変更:
  新設 test-pyramid.json          層の宣言（確かめること / 確かめないこと / 上限）
  新設 scripts/test-pyramid.mjs   check / report
  新設 scripts/lib/glob.mjs       glob の解釈を1か所へ寄せた
  scripts/web-boundaries.mjs      自前の glob を削除し lib/glob.mjs を使う
  scripts/web-strict.mjs          同上。2つの実装で `**` の意味が違っていた
                                  （境界側は1階層以上、strict 側は0階層可）。
                                  0階層可のほうへ揃えた。取りこぼしは緑として
                                  出るので、広いほうが安全側である
  package.json                    test:pyramid / test:pyramid:report を追加。
                                  verify:web の ci:parity の次へ入れた
  .github/workflows/ci.yml        web ジョブへ Tests sit in the layer ... を追加
  README.md                       「テストの層」節を新設。ゲート表へ1行追加。
                                  verify:web の説明と「よく使うコマンド」も更新
  docs/adr/005_20260911_test_pyramid.md  判断の記録（採らなかった案を含む）
  docs/README.md                  計画の置き場所が `x-plan/` のままだったのを修正

追加したテスト:
  scripts/tests/test-pyramid.test.mjs 11件
    exclude を書いた層はより狭い層に譲る / 未分類を報告する /
    二重所属を報告する / script 不在を報告する / 空の層を報告する /
    食い違いが無ければ内訳を返す / 上限超過を理由付きで報告する /
    上限の無い層には何も言わない / 知らない数え方で黙って0件にしない /
    node_modules・.venv の中を自分たちのテストと数えない /
    註釈の中の def を数えない・describe と beforeEach を数えない
  scripts/tests/glob.test.mjs 6件
    3つのゲートが同じ意味で glob を読むこと（`*` は1階層 / `**` は0階層可 /
    末尾 `/**` は配下すべて / `.` を正規表現として解釈しない / 全体一致）

意図的に壊したテストと確認結果:
  services/newtests/test_x.py を置く
    → 「どの層にも属していない」で exit 1。削除して緑に戻ることを確認
  e2e の上限を 30 → 17 へ一時的に下げる
    → 「e2e: 18 件は上限 17 件を超えている」と理由を添えて exit 1
  ci.yml から test:pyramid のステップを削除
    → ci:parity が「web job: test:pyramid が走っていない」で exit 1。
      新しいゲートも W12-1 の仕組みに載っていることの確認になっている
  glob を寄せる前後で web-boundaries report の出力を差分比較 → 差分なし。
  strict の範囲も「範囲外の残件 365 件」で変わらず

実行した確認コマンドと結果:
  node --test 'scripts/tests/**/*.test.mjs' → 49 pass（既存32 + 新規17）
  pnpm run test:pyramid → 「121 ファイルすべてが宣言どおりの層にある」
  pnpm run test:pyramid:report → gates 3/32・ml 38/813・services 9/133・
    tools 5/69・web-unit 11/162・web-unit-vendor 50/111・e2e 3/18（上限30）
  pnpm run ci:parity → 「手元の検証 16 件すべてが ci.yml で走る」
  pnpm run verify:web → 緑（Test Files 60 + 1・ESLint 3198/1087・境界 217）

説明できるようになったこと:
  test pyramid は「件数の比率」ではなく「層ごとの約束」であること。
  比率をゲートにすると、E2E を増やしたいときに無関係な単体テストを増やせば
  通ってしまう。守りたいのは上の層で確かめるものの**種類**である
  同じ判断を2つの層に書くと、速いほうが消える理由
  宣言（文章）と実態のずれは、ずれたという理由では表に出てこないこと。
  W12-1 の CI と同じ性質で、直し方も同じ（ずれを検知する側を置く）

まだ説明できないこと:
  parameterized test（subTest / it.each）を使い始めたとき、静的な数え方が
  どれだけずれるか。いまは1件も無いので実測できていない
  mutation testing（stryker）の位置づけ。層ではなく層の検出力を測るもので、
  いまは取り込んだコードの1ファイルだけが対象。W12-9 で決める
  取り込んだ 111 件（web-unit-vendor）を、いつまで回帰の網として抱えるか

次の一手:
  W12-4（failure test）と W12-5（OpenAPI 破壊変更検知）。どちらも W12-3 待ちで、
  層が決まったので着手できる。どの層に置くかは決まっていて、
  failure test は ml / services 単体、OpenAPI は services 単体の隣に新しい層を
  足すかどうかを最初に決める（足すなら test-pyramid.json への宣言が先）
```

### 2026-09-11 — Stage 2 / W12-4（failure test）

```text
日付: 2026-09-11
対象ID: W12-4
今回の範囲: retry / timeout / API停止 / 破損Artifact の4分類について、
            実際に起きる失敗が「何が起きたか読める形」で失敗することを確かめる。
対象外: W12-5（OpenAPI）、W12-6（smoke E2E）。新しい層は作らない。
完了条件: pnpm run verify:python が緑。保護を外すと新しいテストが落ちること。

やったこと:
  先に数えた。失敗に言及するテストは着手前に 233 件あり、「失敗テストが無い」
  という話ではなかった。そこで4分類それぞれについて**実装を読んで**、
  守られていない経路を探した。見つかったのは4つで、いずれも
  「失敗はするが、何が起きたか読めない」種類だった。

  1. 破損Artifact（2か所）
     services/prediction_api/model_source.py と ml/pipeline/steps.py の
     どちらも joblib.load を素で呼んでいた。実測すると、壊れたファイルは
     KeyError: 0、途中で切れたファイルは EOFError になる。どちらも
     ファイル名もモデル名も言わない。前者は起動時なので再起動ループの
     ログに、後者はagentのTaskログに、その一行だけが残る。
     ml/pipeline/artifacts.py は同じことを既に丁寧にやっていた
     （read_split / read_json）。**壊れ方を語る側と語らない側が
     同じリポジトリに同居していた。**

  2. API停止（serving の起動）
     _by_id は例外を ModelUnavailableError に包んでいたが、
     _in_production（query_models）は包んでいなかった。ClearML が
     止まっているときだけ SDK の型のまま落ちる。モデル側の問題と
     サーバ側の問題が見分けられないと、止めて直すのか待つのかを選べない。

  3. retry（1か所だけ実在した）
     grep して分かったのは、このリポジトリで retry するのは
     tools/seed_clearml.wait_until_ready の1か所だけだということ。
     60回×2秒。ここが無検査だった。他は「一度で失敗して再起動される」
     設計で、これは意図的である（README・各moduleのdocstringと一致）。

  4. timeout
     provenance の git 呼び出し（5秒）と server_health（10秒）は
     期限を持っていたが、その期限を確かめるテストが無かった。
     provenance のテストは全て run_command を差し替えており、
     本物の _run_git を呼ぶテストが1つも無かった。

自分で加えた変更:
  services/prediction_api/model_source.py
    _read_weights() を新設し joblib.load を包んだ。モデルID・パス・
    例外の型と本文を1行にする。_in_production の query_models も包んだ
  ml/pipeline/steps.py
    _load_pipeline の joblib.load を包んだ。同じ形の説明にする
  docs/runbooks/004_...prediction_service.md
    トラブルシュート表へ3行（EOFError / KeyError: 0 / registry cannot be read）
  docs/runbooks/002_...training_pipeline.md
    同じく1行（重みが転送で壊れたとき）

追加したテスト（21件）:
  services/tests/prediction_api/test_model_source.py 3件
    Registryが読めないときに、モデルの問題と見分けられる形で止まる /
    途中で切れた重みが EOFError のままにならない / joblib以外のファイルは
    パスを添えて refuse される
    既存1件（weights cannot be fetched）は FileNotFoundError を期待していた。
    保護を入れたので ModelUnavailableError へ直した。**期待の書き換えは
    1件だけで、それ自体が今回直した実害である**
  ml/tests/pipeline/test_steps.py 2件
    転送で切れた重み / モデルではないファイル
  ml/tests/semiconductor_quality/test_provenance.py 5件（新クラス GitCommandTest）
    期限が渡されている / 時間切れは unknown / git が無い / リポジトリでない /
    止まった git が学習そのものを止めない
  tools/tests/test_seed_clearml.py 5件（新クラス WaitUntilReadyTest）
    すぐ答えたら待たない / 立ち上がり中は待つ / 答えたらそこで止める /
    諦めるときは host を言う（60回） / 500 も「まだ」として扱う
    time.sleep は差し替えた。確かめたいのは待ち方であって2分かかることではない
  tools/tests/test_semiconductor_seed.py 6件
    readiness は期限付きで訊く / 200以外は host を添えて refuse /
    サーバが無ければ exit 1 と理由、かつ Gateway を作らない /
    時間切れも同じ / 「まだ準備できていない」も同じ

意図的に壊したテストと確認結果:
  model_source の _read_weights を外す → 3件が KeyError: 0 と
    FileNotFoundError のまま落ちる（= 保護が無いと何も語らない、の再現）
  steps の同じ保護を外す → 2件が失敗
  wait_until_ready の RuntimeError を return に変える → 1件が失敗
    （黙って諦めたことを検出できている）
  provenance から timeout= を外す → 1件が失敗
  4件とも復元して緑を確認した

実行した確認コマンドと結果:
  pnpm run verify:python → 緑
    ruff / mypy（158ファイル）/ ml 820 + services 136 + tools 80 = 1036 tests
    （着手前 813 / 133 / 69）。カバレッジ 88%
  pnpm run test:pyramid → 121 ファイルすべてが宣言どおりの層にある
    （W12-3 の宣言のまま。新しい層は要らなかった）

説明できるようになったこと:
  「失敗のテストがある」と「失敗が読める」は別だということ。
  233 件の失敗テストがあっても、壊れたファイルは KeyError: 0 で落ちていた。
  数を数えても分からず、実装を読むまで見つからない種類の穴である
  このシステムが retry しない設計であること、そしてその例外が1か所だけ
  （seedの起動待ち）あること。他は「一度で失敗して再起動」に寄せてある
  同じリポジトリの中で、丁寧に壊れ方を語る層（ml/pipeline/artifacts）と
  語らない層（重みの読み込み）が並んでいたこと。既にある良い書き方を
  基準にすると、穴は「無い機能」ではなく「揃っていない扱い」として見える

まだ説明できないこと:
  ops_exporter が部分的にしか読めないとき（queuesは読めてworkersが落ちる）の
  扱い。いまは全体が失敗して clearml_reachable=0 になる。0 と「部分的に古い」
  を区別すべきかは、実運用のアラートを設計するまで決められない
  prediction_api が起動後にClearMLを見ないこと（モデルは起動時に1回）。
  これは意図した設計だが、Registryが壊れたことに気付く手段が無い

次の一手:
  W12-5（prediction_api の OpenAPI スナップショットと破壊変更検知）。
  00 §5.1.4 のとおり consumer-provider にはしない。
  置き場所は services 単体の層で足りる見込み（新しい層は要らない）。
```

### 2026-09-11 — Stage 2 / W12-5（OpenAPI スナップショットと破壊変更検知）

```text
日付: 2026-09-11
対象ID: W12-5
今回の範囲: prediction_api が公開しているAPIの形を記録し、黙って変わったら
            失敗する状態にする。破壊変更かどうかを報告に分けて出す。
対象外: consumer-provider contract test（00 §5.1.4）。W12-6（smoke E2E）。
        新しいテストの層も、新しいゲート script も作らない。
完了条件: pnpm run verify が緑。APIを変えると services:test が落ちること。

先に確かめたこと:
  消費者を数えた。apps/web/src/app/features/serving/ は empty-state 1つだけで、
  画面は推論APIを呼んでいない。呼んでいるのは runbook 004 の curl と
  services/tests だけである。**片側しかいない契約は書けない**ことを、
  数えてから確認した。

やったこと（判断3つ）:
  1. 検知は完全一致、分類は説明
     破壊変更（応答が消えた・要求に必須の値が増えた）と互換の変更
     （エンドポイントが増えた）を分けて報告するが、**失敗の判定は文書の
     完全一致**にした。分類は上下限や enum の狭まりを読まない。
     分類を判定に使うと、**読めない種類の変更が緑になる**。
     W12-1（CIから落ちても緑）と同じ形の失敗である。
     分類できない差分は「差分を読むこと」と書いて出す。

  2. 破壊の定義は、送る側と読む側で逆向きになる
     要求の値が「任意 → 必須」になれば送り手が壊れ、応答の値が
     「必須 → 任意」になれば読み手が壊れる。同じ規則を両側へ当てると
     片方を必ず取りこぼす。confidence: number | null のような値は
     型を null|number として持つ（null を落とすと、読み手が落ちる変更が
     互換に見える）。

  3. ゲートにしない
     新しい npm script のゲートにすると ci.yml へ写す必要が生まれ、
     ci:parity の対象が1つ増える。services:test の中に置けば
     verify:python と CI の python ジョブが既に走らせている。
     **検証を増やすために、走らせる場所を増やさない。**
     test-pyramid.json も層は増やさず、services-unit の「確かめること」に
     1行足しただけで済んだ。

自分で加えた変更:
  services/contracts/openapi.py（新規）
    OpenAPI 文書を「呼び出し側が依存できる部分」へ縮め、2つを比べて
    破壊/互換に分ける。$ref を辿り、入れ子は model.model_version、
    配列は measurements[].sample_id という呼び出し側が書く名前へ平らにする。
    自己参照するスキーマで止まらないよう、辿った $ref は二度辿らない。
    prediction_api 専用にしなかったのは、ops_exporter が API を公開する
    ようになったときに同じ形で守れるようにするため
  services/prediction_api/contract.py（新規）
    文書の生成・記録の読み書き・差分の報告
  services/prediction_api/contract_cli.py（新規）
    pnpm run serving:openapi:update の中身。引数を取らない
  services/prediction_api/openapi.json（新規・472行）
    公開しているAPIの記録。コードから生成されるので手で書かない
  package.json に serving:openapi:update を追加（verify には入れない）
  test-pyramid.json の services-unit の verifies に1行
  docs/adr/006_20260911_published_api_snapshot.md（新規）
  README.md（推論サービスの節に「公開しているAPI」）、
  docs/runbooks/004、services/README.md

  資格情報を持たない呼び出し側を1件だけ宣言して文書を作っている。
  サービスは呼び出し側が無いと起動を拒む（ADR 002）が、文書の中身は
  呼び出し側が誰かに依存しない。組み立てには足り、誰も認証できない。

追加したテスト（33件）:
  services/tests/contracts/test_openapi.py 21件（新規）
    surface 5件: 操作の名前 / 入れ子と配列の名前 / $ref を辿る /
      自己参照で止まらない / 省略可能な値が null を落とさない
    変わらない 2件: 同じ文書 / summary は約束ではない
    操作 2件・要求 6件・応答 6件: 上の「逆向き」の表を1行ずつ
  services/tests/prediction_api/test_contract.py 12件（新規）
    公開している文書 3件: 記録とコードが一致する（これが本体のゲート）/
      3つのエンドポイントが全部書かれている / 資格情報なしで文書が作れる
    報告 6件: 動いていなければ黙る / 消えた操作は破壊 / 増えた操作は互換 /
      必須の値が増えたら破壊 / 分類できない変更も報告される / 直し方を言う
    記録の書き出し 1件・コマンド 2件

意図的に壊したテストと確認結果:
  schemas.Measurement に必須の lot_id を足す →
    「呼び出し側が壊れる変更 / POST /predict: 要求に必須の
    measurements[].lot_id が増えた」で落ちる
  openapi.json から /ready を消す → 「互換の変更 / GET /ready: この操作が
    増えた」（記録側を古くすると、コードが足した形になる）と、
    エンドポイント一覧のテストの2件が落ちる
  どちらも復元して緑を確認した。

  報告は fail() へそのまま渡している。assertIsNone に載せると repr で
  1行に畳まれ、読ませたい複数行がエスケープされた文字列になる。
  **読める失敗**（W12-4）はテスト側の書き方にも要る。

実行した確認コマンドと結果:
  pnpm run verify → 緑
  pnpm run verify:python → ruff / mypy（165ファイル）/
    ml 820 + services 169 + tools 80 = 1069 tests（着手前 820 / 136 / 80）
  pnpm run test:pyramid → 123 ファイルすべてが宣言どおりの層にある（層は増やしていない）
  pnpm run ci:parity → 手元の検証 16 件すべてが ci.yml で走る（ci.yml は変更なし）

説明できるようになったこと:
  消費者がいない API でも守れることがあり、それは「形」ではなく
  「形が黙って変わること」だということ。守る対象を言い換えると、
  片側しかいない契約を書かずに済む
  スナップショットの価値は検知であって、差分の分類ではないこと。
  分類は「472行の JSON のどこが効くのか」を人が読むためにある。
  分類を判定に使った瞬間、分類できない変更が緑になる
  破壊の向きが送り手と読み手で逆になること。表にすると6行しかないが、
  頭の中だけでやると必ずどれかを取りこぼす

まだ説明できないこと:
  上下限や enum の狭まりを分類できるようにするかどうか。いまは
  「分類できない差分」として出る。誤読の実害が出てから決める
  消費者が現れたとき、openapi.json からクライアントを生成するのか、
  consumer-driven な契約へ移すのか。W12 の範囲では決めない
  /metrics は include_in_schema=False のまま文書に載っていない。
  Prometheus が読むのは exposition format なので載せる理由が無いが、
  「公開しているのに記録に無いもの」が1つあることは覚えておく

次の一手:
  W12-6（smoke E2E: Dataset登録 → Queue学習 → 比較 → 昇格 → 推論）。
  W12-2 で e2e が CI に載っているので前提は揃っている。
  E2E の上限は30件・現在18件で、smoke に見込んでいるのは5件程度（00 §5.1.5）。
  下の層で確かめられることをここへ書かないこと。
```

### 2026-09-12 — Stage 2 / W12-6（smoke E2E）

```text
日付: 2026-09-12
対象ID: W12-6
今回の範囲: Dataset登録 → Queue学習 → 比較 → 昇格 → 推論への受け渡しが
            縦に繋がっていることを、5件のE2Eで見る。
対象外: W12-7（a11y）。業務ルールの網羅。指標の値の読み方。
        推論そのもの（画面から到達できない。下記「先に確かめたこと」）。
完了条件: pnpm run web:e2e が緑（18 → 23件）。test:pyramid の上限（30件）内。

先に確かめたこと:
  画面は prediction_api を呼んでいない（W12-5 で数えたとおり）。
  ブラウザから推論へは到達できないので、**最後の節目をどこに置くか**を
  先に決めた。ユーザ判断で「昇格の受け渡し点まで」とし、推論そのものは
  services-unit と scripts/serving-smoke.sh の範囲だと明記した。
  E2E から推論APIを叩く案は採らない。backend 起動が前提になり、
  W12-2 で作った「fixtureだけで走る e2e ジョブ」が壊れる。

  画面が何を問い合わせるかは、推測せずに実測した（使い捨ての spec を置いて
  endpoint と要求の中身を出す）。compare の URL が行列パラメータ
  （compare-tasks;ids=a,b）であることも、Dataset版の一覧が
  type=data_processing で引くことも、ここで分かった。

やったこと（判断3つ）:
  1. 1つのfixtureが1つの世界を返す
     既存の2つのfixtureは画面ごとに必要な応答を返す。それでは各画面が
     「自分用の世界」を見るので、節目の間で取り違えが起きても全部緑になる。
     smoke は Dataset版 1.0.0 → その版で学習した実行 → 比較 →
     stage:production のモデル → そのバージョン文字列、を1つの世界として持つ。

  2. fixtureは絞り込みを実際に当てる
     id / project / type / name（正規表現）/ 続きの問い合わせ（scroll_id）を
     読んで返す。無視して常に全部返すと、画面が誤った条件で問い合わせていても
     気付けない。**応答を単純にすると、画面ではなくfixtureを確かめることになる。**
     続きの問い合わせに同じものを返すと、一覧に同じ行が積み上がった（実測）。

  3. 比較では指標の値を見ない
     どちらが良いかの読み方は ml-unit と web-unit にある（ADR 005）。
     ここで見るのは「取り違えずに2つ並ぶか」だけで、並んだのがその2つかは
     行が指す Task の id で見る。ClearML は実行のたびに同じ名前の Task を
     作るので、名前では区別できない。

自分で加えた変更:
  apps/web/e2e/fixtures/smoke.fixture.ts（新規）
    1つの世界と、絞り込みを当てる応答。Python側の既定値
    （dataset_project / queue 名 / MODEL_NAME / stage タグ）と揃えてある
  apps/web/e2e/smoke.spec.ts（新規・5件）
  test-pyramid.json の e2e の maxReason（現在23件へ）
  README.md の E2E 節に smoke の説明（最後の節目が受け渡し点である理由）
  製品コードは変更していない

追加したテスト（5件）:
  1. 登録: Dataset版の一覧に semiconductor-quality-data v1.0.0 が出る。
     学習の実行がそこに並ばない
  2. 学習: semiconductor-training の行に実行が入り、semiconductor-pipeline の
     行には入らない（行と実行の対応が取れていること）
  3. 比較: 比較画面が候補と提供中の2つの Task を指している（idで確認）
  4. 昇格: 一覧で stage:production が1つ、stage:staging が1つ
  5. 受け渡し: 画面が名乗るモデルのバージョンが昇格したものであり、
     候補（staging）のものではない

意図的に壊したテストと確認結果:
  Queue の entries を pipeline 側へ移す → 2 が落ちる
  Dataset版の runtime.version を 9.9.9 にする → 1 が落ちる
  比較の問い合わせから片方の Task を落とす → 3 が落ちる
  モデルの stage:production を staging にする → 4 と 5 が落ちる
  models の tags 絞り込みを外して候補を先頭にする → 5 が落ちる
  すべて復元して 23/23 緑を確認した。

  映らなかった break も1つある。tasks の type 絞り込みを外しても
  Dataset版の一覧は変わらなかった（画面側でも絞っているため）。
  **fixture を緩めても落ちない経路があること**は記録しておく。

実行した確認コマンドと結果:
  pnpm run web:e2e → 23 passed（着手前 18）
  pnpm run web:e2e:typecheck → 緑
  pnpm run test:pyramid → 124 ファイルすべてが宣言どおりの層にある
  pnpm run test:pyramid:report → e2e 4ファイル / 23ケース / 上限30

説明できるようになったこと:
  E2E で確かめられるのは「縦に繋がっているか」であって、繋がりの証拠は
  **同じ1つの世界を複数の画面が矛盾なく映すこと**だということ
  fixture の作り方そのものがテストの強さを決めること。絞り込みを当てない
  fixture は、画面の問い合わせが誤っていても緑になる
  到達できない節目は、E2E を伸ばすのではなく**どこまでが画面の責任か**を
  書いて下の層へ渡すこと

まだ説明できないこと:
  比較画面（ClearML Web 由来）の指標の表は、fixture 越しでは行が出ない。
  要求と応答は通っていて、コンソールにも例外は出ない。取り込んだ画面の
  内部の話で smoke の判断には使っていないため、原因は追っていない。
  自作画面で同じことが起きたら追う
  E2E は残り7件（上限30・現在23）。W12-7 の a11y でどれだけ使うか

次の一手:
  W12-7（a11y 試験・axe）。E2E の枠は5件程度を見込む。
  同じく「下の層で確かめられることを書かない」が効く。
  規則そのもの（コントラストや属性の付け方）は lint と単体で見られるので、
  E2E へ持ち上げるのは画面として組み上がったときにだけ分かるものに限る。
```

### 2026-09-12 — Stage 2 / W12-7（a11y 試験・axe）

```text
日付: 2026-09-12
対象ID: W12-7
今回の範囲: 自作画面が支援技術から使える形で組み上がっているかを axe で見る。
対象外: 取り込んだ ClearML Web の外枠（凍結領域）。規則そのものの学習。
        W12-8 以降。
完了条件: pnpm run web:e2e が緑（23 → 27件）。自作の範囲の違反が0件であること。

先に確かめたこと:
  画面全体へ axe を当てて、いま何が出るかを数えた。
    critical aria-allowed-attr 2 / critical button-name 2 /
    serious link-name 7 / moderate landmark-one-main 1 / moderate region 13
  いずれも取り込んだ外枠（左のナビ・ヘッダ）のもので、
  自作画面（sm-quality-pipeline-page の中）は**0件**だった。
  **足りないのは違反を直すことではなく、0件が0件のままであることの固定**だと
  分かったので、baseline は作らなかった。

やったこと（判断3つ）:
  1. 対象は自作の範囲だけ。baseline を作らない
     画面全体を対象にして baseline を置くと、自作の違反が取り込み分の中に
     紛れる。Stage 1-B3（自作分は baseline に入れず直す）と同じ形にした。
     外枠は web-boundaries.json と同じ凍結領域で、直しても取り込み直しの
     たびに消える。

  2. 状態ごとに見る
     空の状態（複製元もモデルも無い）と失敗の状態（ClearMLが答えない）は、
     別の文言・別の要素で描かれる。埋まった状態だけを見ると、
     そこにしか出ない違反を見逃す。

  3. 失敗は規則名と要素まで出す
     件数の比較にすると、赤になったときに何を直せばよいか分からない。
     impact・規則id・件数・セレクタを並べて配列で比べる形にした
     （W12-4 の「読める失敗」と同じ）。

自分で加えた変更:
  apps/web/e2e/accessibility.spec.ts（新規・4件）
  apps/web/package.json に @axe-core/playwright（devDependencies）
  test-pyramid.json の e2e の maxReason（現在27件・残り3件は予備へ）
  README.md の E2E 節に a11y の対象範囲
  製品コードは変更していない（自作画面の違反が0件だったため）

追加したテスト（4件）:
  1. 既定の状態で自作画面に違反が無い
  2. 何も無い状態でも違反が無い
  3. ClearML が答えないときの説明が role="alert" で届き、違反も無い
  4. キーボードだけで版を入れて起動できる（版の次のTabが Start で、
     Enter で tasks.enqueue が1回）

意図的に壊したテストと確認結果:
  自作画面へ alt の無い <img> を足す → 1・2・3 が
    「critical: image-alt (1) img[src$="favicon.ico"]」で落ちる。
    規則と要素が出るので、どこを直すかがそのまま読める
  復元して 27/27 緑を確認した。

  映らなかった break が1つある。名前の無い <button> を足しても落ちなかった。
  中身が無いため大きさが0になり、axe が見えない要素として飛ばすからである。
  **axe が見るのは「見える要素」だけ**で、隠れている操作子は対象外になる。

実行した確認コマンドと結果:
  pnpm run web:e2e → 27 passed（着手前 23）
  pnpm run web:e2e:typecheck → 緑
  pnpm run test:pyramid → 125 ファイルすべてが宣言どおりの層にある
  pnpm run test:pyramid:report → e2e 5ファイル / 27ケース / 上限30

説明できるようになったこと:
  a11y の検査は「全体に当てて数を減らす」形にすると、自作の退行が
  取り込み分に紛れること。**範囲を絞って0件で固定するほうが強い**
  キーボードで辿り着けるかは axe では分からないこと。規則への適合と
  操作できることは別で、後者は組み上がった画面でしか確かめられない

まだ説明できないこと:
  取り込んだ外枠の critical 2種を、いつ・どこまで直すか。いまは凍結領域と
  して触らない方針だが、利用者から見れば同じ1つの画面である
  自作画面が増えたときに、対象の根（いまは1つ）をどう宣言するか。
  画面ごとに spec を足すのか、根の一覧を持つのか

次の一手:
  W12-8（負荷試験・k6）。prediction_api 相手で e2e とは独立している。
  k6 はこの環境に入っていないので docker で走らせる（ユーザ判断）。
  backend 起動が前提になるため verify にも CI にも載せない。
```

### 2026-09-12 — Stage 2 / W12-8（負荷試験・k6）

```text
日付: 2026-09-12
対象ID: W12-8
今回の範囲: 配備された prediction_api が、決めた期限の内側で答え続けるかを
            k6 で見られるようにする。期限は運用が鳴らす値と同じにする。
対象外: モデルの精度、ClearML Server の性能、長時間の耐久・スケール。
        verify / CI への組み込み（backend と推論サービスの起動が前提）。
完了条件: pnpm run web:gates:test が緑（期限の一致を含む）。
          runbook 004 から走らせ方が読めること。

先に確かめたこと:
  期限を自分で決める前に、既にどこかで決まっていないかを見た。
  infra/observability/alerts.yml が持っていた。
    PredictionLatencyHigh: 95パーセンタイル > 1 秒 が 10 分続いたら警告
    PredictionErrorRateHigh: サービス側の失敗 > 5% が 5 分続いたら critical
  **足りないのは数字ではなく、その数字で測る道具だった。**

やったこと（判断3つ）:
  1. 期限は新しく決めず、運用が鳴らす値を借りる
     試験用の数字を別に決めると、同じことを決めた場所が2つになる。
     2つあるものは片方だけが動き、動いたことは表に出てこない。
     「試験は緑なのに本番は鳴る」はその形の失敗である。

  2. ずれを検知する側は、走らせる場所を増やさずに置く
     負荷試験そのものは起動が前提で verify にも CI にも載せられない。
     だが**数字が一致していることは何も立てずに読める**。
     scripts/tests/perf-thresholds.test.mjs を gates の層へ置いた
     （web:gates:test は verify と CI の中で既に走っている）。
     単位が違う（秒とミリ秒）ので、揃えずに比べて1000倍ずれたまま
     一致と読まないこともテストにした。

  3. k6 は入れずに docker で走らせる
     手元へ入れると「その機械にだけ入っている検査」になる。
     CI に載せない検査だからこそ、誰でも同じものを走らせられるほうが効く。

自分で加えた変更:
  services/perf/prediction_api.js（新規）
    constant-vus。/ready で掴んだ model_version と、走行中の応答が
    同じであることも見る（途中で入れ替わったなら前半と後半は別物を測っている）
  scripts/load-test.sh（新規）
    docker で k6 を走らせる。資格情報は serving:smoke と同じ .generated から。
    docker が無い / daemon が動かないときは、その旨で止まる
  scripts/tests/perf-thresholds.test.mjs（新規・5件）
  package.json に serving:load（verify には入れない。ci:parity は 16 件のまま）
  docs/runbooks/004 に「負荷を掛けて確かめる」、README.md に1行

追加したテスト（5件・gates 49 → 54）:
  期限の一致 2件（秒とミリ秒の換算を含む）
  k6 の options が定数をそのまま使っている 1件
    （定数だけ合わせて options に直書きされると、効くのは options のほう）
  アラートごとに区切って読む 1件（隣のアラートの数字を読まない）
  読めないときに黙って通さない 1件

意図的に壊したテストと確認結果:
  alerts.yml の 95パーセンタイルを 1 → 2 にする →
    「応答の期限が、鳴らすと決めた値と同じである」が落ちる。復元して 54/54 緑。

実行した確認コマンドと結果:
  pnpm run web:gates:test → 54 pass（着手前 49）
  pnpm run ci:parity → 16 件（serving:load は verify の外なので増えない）
  sh -n scripts/load-test.sh → 構文 OK
  node --check（k6 スクリプトを .mjs として）→ 構文 OK

  **負荷試験そのものは、着手時点のこの環境では走らせていない。**
  docker クライアントはあるが daemon へ繋がらず（`Cannot connect to the
  Docker daemon`）、権限も無い。走らせたときに初めて分かること
  （実際の p95、10 VU で足りるのか）は未確認である。

  → 2026-09-12 の通し確認で docker daemon が動いたため実走した。
    pnpm run serving:load → LOAD TEST OK
      10 VUs × 30s / 417 req / 失敗 0 / checks 1248 すべて成功
      p95 = 808.13ms（期限 1000ms の内側）、med = 729.05ms、max = 883.29ms
    借りた期限（alerts.yml の 95パーセンタイル > 1 秒）で実際に測れており、
    10 VU では余裕が 200ms 程度しかないことも分かった。**余裕は大きくない。**
    VU を増やしたときにどこで超えるかは、まだ測っていない。

説明できるようになったこと:
  性能の「合格」は試験が決めるものではなく、運用が鳴らすと決めた値を
  借りるものだということ。借りれば、試験と本番が同じことを言う
  走らせられない検査でも、**一致だけなら何も立てずに見られる**こと。
  検証を増やすために走らせる場所を増やさない（W12-5）の別の形である

まだ説明できないこと:
  10 VU / 30秒 が妥当かどうか。実走していないので、いまの数字は
  「動かすための既定値」でしかない。最初に走らせた人が runbook を直すこと
  1秒という期限が、1ロット10件の要求に対して緩いのか厳しいのか
  CI で走らせる形（推論サービスだけを立てて測る）を作るかどうか

次の一手:
  W12-9（flaky test の検知・原因修正・再発防止の運用）。
  隔離しない。検知の仕組みを先に置く。
```

### 2026-09-12 — Stage 2 / W12-9（flaky の検知と運用）

```text
日付: 2026-09-12
対象ID: W12-9
今回の範囲: 振れるテストを見つける道具と、見つけたときの手順・記録の置き場所。
対象外: 隔離（skip / quarantine）、retry の導入、CI への常時組み込み。
完了条件: pnpm run test:flaky で振れたテストが1件ずつ出ること。
          その道具自身が gates の層で検査されていること。

先に確かめたこと:
  Playwright の retries は 0 のままだった（W12-2 で載せたときの設定）。
  つまり振れたテストは既に「赤」として出る状態であり、**足りないのは
  隠す仕組みではなく、赤が毎回なのか時々なのかを数える手段**だった。
  E2E は既に JUnit XML を書いている（apps/web/reports/e2e/playwright.xml）。
  数えるための報告を新しく作る必要は無かった。

やったこと（判断4つ）:
  1. 隠さない
     隔離すると赤が消え、直す理由も消える。retry を入れると振れたテストが
     緑になり、検知そのものが無くなる。このシステムは失敗を一度で確定させる
     （W12-4）。テストも同じ扱いにする。

  2. 判定の単位はテスト1件
     走行の成否で数えると、1件振れただけで全部が振れたことになる。
     JUnit XML を読み、testsuite（ファイル）と testcase を繋いだ名前で持つ。
     別のファイルの同名テストを1つに畳まないため。

  3. ずっと赤は flaky ではない
     混ざったものだけを flaky と呼び、常に赤は「壊れている」として分ける。
     走行ごとに実行された集合が違うことも別に出す（名前が変わった /
     途中で落ちて残りが走らなかった）。原因も直し方も違う。

  4. CI では走らせない。対象は E2E だけ
     同じ試験を5回走らせる分だけ遅くなる。振れているかは疑いが出てから
     数えれば足りる。下の層は時刻・並行・外部を持たない設計なので、
     そこが振れるなら flaky ではなく設計の綻びである。

自分で加えた変更:
  scripts/flaky.mjs（新規）
  scripts/tests/flaky.test.mjs（新規・8件）
  package.json に test:flaky（verify には入れない。ci:parity は 16 件のまま）
  docs/runbooks/009_20260912_flaky_tests.md（新規）
    方針・数え方・原因の4分類・台帳（いまは空）・トラブルシュート
  README.md のテストの層の節に3行

追加したテスト（8件・gates 54 → 62）:
  読み取り 4件: テスト1件ずつ / ファイルごとに名前を分ける /
    skipped を緑と数えない / error も失敗として読む
  畳み方 2件: 混ざったものだけ flaky・ずっと赤は failing /
    走行ごとに集合が違うことを見逃さない
  報告 2件: どのテストが何回緑だったかまで出す / 振れていないときはそう言う

  **このテストが1件、実装の誤りを捕まえた。** 自己終了タグ
  （<testcase ... />）の属性を貪欲に読むと `/` まで属性として飲み込み、
  次の </testcase> までを中身として読む。結果、隣の skipped を自分のものと
  誤読して「走っていない」と数えていた。**振れているのに振れていないと言う**
  種類の誤りで、隔離より質が悪い（誤った緑は何も残さない）。

意図的に壊したテストと確認結果:
  50% で落ちる使い捨ての spec を e2e へ置き、--times 4 で実行 →
    「振れたテスト（4 回中）: 3/4 緑  _coin.spec.ts › coin」と出て、
    終了コード 1。隔離しないこと・runbook 009 へ記録することも一緒に出る。
    使い捨ての spec は削除した。

実行した確認コマンドと結果:
  pnpm run test:flaky --times 3 → 振れたテストは無い（27 件 × 3 回すべて緑）
  pnpm run web:gates:test → 62 pass（着手前 54）
  pnpm run test:pyramid → 127 ファイルすべてが宣言どおりの層にある
  pnpm run ci:parity → 16 件（test:flaky は verify の外なので増えない）

説明できるようになったこと:
  flaky への手当ての大半（隔離・retry）は、**検知を捨てることで静けさを買う**
  ものだということ。赤を消すのではなく、赤の頻度を数える側へ置く
  「ずっと赤」と「時々赤」を同じ箱に入れないこと。前者は普通の不具合で、
  後者だけが原因の分類（時間 / 順序 / 外部 / 実装の非決定性）を要する
  台帳が空であることと、振れるテストが無いことは違うということ。
  数えていない期間のことは分からない

まだ説明できないこと:
  何回繰り返せば「振れていない」と言えるのか。いまの既定は5回で、
  根拠は「手元で待てる時間」でしかない
  下の層（unit）が振れ始めたときに、この道具を向けるのか、設計の綻びとして
  別に扱うのか。いまは E2E 固定にしてある
  mutation testing（stryker）の位置づけは W12-3 から持ち越しのまま。
  層ではなく層の検出力を測るもので、まだ決めていない

次の一手:
  W12-10（ADR 化）。standalone / Signals・NgRx の責務 / lazy route /
  error handling。README を正としてそこから起こす。二重に書かない。
```

### 2026-09-12 — Stage 2 / W12-10（ADR 化）

```text
日付: 2026-09-12
対象ID: W12-10
今回の範囲: W11 から持ち越していた4つ（standalone / Signals・NgRx の責務 /
            lazy route / error handling）を ADR にする。
対象外: 形（ディレクトリ構成・feature の標準形・命名）。README が正である。
完了条件: ADR が README と重複していないこと。README から ADR を指せること。

先に確かめたこと:
  apps/web/README.md に何が既にあるかを読んだ。形は書かれている
  （ディレクトリ構成・feature の標準形・変更検知と命名・削除済みの死骸）。
  **書かれていなかったのは理由と代案**である。
  4つのうち error handling だけは、形も理由もどこにも無かった。
  実装（quality-pipeline.effects.ts）にだけ存在していた。

やったこと（判断2つ）:
  1. ADR は形を書き写さない
     README と ADR の両方に形を書くと、片方を直したときにもう片方が古くなる。
     W12-1 以降ずっと言っている「宣言が2つあると片方だけが動く」であり、
     **文章同士でも同じことが起きる**。ADR は理由と代案だけを持ち、
     形は README を指す。README からも ADR を指して往復できるようにした。

  2. コードにしか無い判断を起こす
     失敗の扱いは実装にだけあった。
       Effects は catchError で必ず action へ変換する（例外を外へ投げない）
       失敗の重さで分ける（指標の取得失敗だけ EMPTY にし、画面は失敗にしない）
       成功したら error を消す。失敗しても直前の結果は消さない
       meta.result_msg → 状態コード0は通信の説明 → どちらも無ければ既定の文
     ADR 化の効用は、こういう判断が1つ見つかることでもある。

自分で加えた変更:
  docs/adr/007_20260912_angular_feature_composition.md（新規）
  README.md（Angular の節から ADR 007 へ）
  apps/web/README.md（feature の標準形の節から ADR 007 へ）
  製品コードは変更していない

実行した確認コマンドと結果:
  pnpm run verify → 緑（末尾の「W12 完了時点の確認」を参照）

説明できるようになったこと:
  ADR と README の分担。**形は1か所、理由は1か所**で、互いを指す
  ADR 化が「書き写し」になるときは、たいてい対象を間違えている。
  起こすべきなのは、決めたのに書かれていない判断のほうである

まだ説明できないこと:
  自作 feature が2つ目になったとき、この ADR がそのまま当たるか。
  当たらない判断が出たら、その時点で追記するか別の ADR にするかを決める
  凍結領域の書き方（NgModule・Observable 購読）へ、いつか合流させる日が
  来るのかどうか

次の一手:
  W12 は完了。Stage 2 の残りは W14（IaC と災害復旧）・W15（チーム開発標準）・
  W16（コスト・容量・ライフサイクル）で、Stage 3（ClearML コード読解）は
  まだ Level 0 のまま並行して回せる。
```

### 2026-09-12 — Stage 3 / P0-0（学習ベースラインと安全網）

```text
日付: 2026-09-12
対象ID: P0-0
今回の範囲: 取り込んだ ClearML Web（凍結領域）に手を入れる前の状態を測る。
            test / typecheck / build を再現し、**何が検出され何が素通りするか**を
            意図的な破壊で確かめる。
対象外: 実際の読解（P0-1 以降）。凍結領域の修正。安全網の拡張。
完了条件: 再現可能な数値が取れていること。検出力が実測で分かっていること。

再現したベースライン（3回走らせて同値）:
  pnpm run web:test        60 files / 270 tests 緑（約5秒）＋ widgets 1/3 緑
                           内訳 自作 quality-pipeline 11 files 162 tests
                                取り込み             49 files 108 tests
  pnpm run web:build       緑（約20秒）。警告は scss の budget 超過3件ほか
  node scripts/web-strict.mjs --all
                           移行済み範囲エラー0 / 範囲外の残件 365 件
  pnpm run web:lint:baseline
                           errors 3198/3198, warnings 1087/1087（凍結値と一致）

  注: web-strict.mjs の冒頭コメントは「500件以上」と書いているが実測は 365 件。
      この数字は tsconfig.strict.json の include から import で辿れる範囲の
      話であり、凍結領域の全量ではない。**全量を測った数字ではない。**

安全網の広さ（実測）:
  取り込み側の .ts        1448 本（webapp-common 736 / features 68 /
                                    business-logic 607 ほか）
  そのうち spec を持つ      50 本（3.5%）
  business-logic 607 本は spec が1つも無い
  取り込みの spec 49 files のうち **36 files は "should create" しか無い**
  （108 tests 中 39 tests が "should create"）

意図的に壊して測った検出力（2回。どちらも実行後に復元済み・差分なし）:
  対象 webapp-common/shared/pipes/filesize.pipe.ts（spec 無し・画面が使う）

  probe A 型エラーを入れる（string を number へ代入）
    web:build            ✅ 落ちる（TS2322 / angular-compiler）
    web:test             ✅ 落ちる（ng test が先に AOT build する）
    web:strict           ❌ 素通り（include の外。範囲外は報告しない）

  probe B 振る舞いだけ変える（fileSizeConfigStorage.base を 2 → 10）
    web:test             ❌ 270 tests すべて緑
    web:build            ❌ エラー0
    web:lint:baseline    ❌ 3198/1087 のまま一致
    → 画面上のあらゆる容量表示が KiB 基準から KB 基準へ静かに変わるが、
      **verify:web は最後まで緑のまま**である

説明できるようになったこと:
  凍結領域に対する安全網は**型とビルドの成立までしか見ていない**。
  「壊れたか」ではなく「壊れていても組み上がるか」を見ている。
  だから型を壊す変更は必ず捕まり、振る舞いだけの変更は必ず素通りする。
  これは不足ではなく設計上の線引きである（自作＝判断を持つ / 取り込み＝
  持ち込まない）。ただし**線の位置を測っていなかった**のが今回までの状態。
  270 tests という数字は安心の根拠にならない。その6割は自作の分であり、
  取り込み側の実質は「構築できる」の確認 36 files 分である。
  E2E も同じ線の内側にいる。取り込んだ比較画面が fixture 越しで指標の表を
  描かない（W12-6 の記録）ことに smoke が気付かないのは、smoke が
  取り込み画面の中身を判断に使っていないためで、これも仕様どおりである。

まだ説明できないこと:
  凍結領域の振る舞いを守る手立てを**持つべきかどうか**。
  持つなら置き場所（characterization test を web-unit-vendor へ足すのか、
  test-pyramid.json が禁じている「取り込みへ新しいテストを足さない」を
  改めるのか）。いまは判断しない。P0-1 以降で実際に触る必要が出てから決める
  strict の残件 365 件が、凍結領域の全量に対して何割なのか

次の一手:
  P0-1（Task一覧・詳細を route → API → state → UI で1本 trace する）。
  probe で分かったとおり、trace の答え合わせに使えるのは build と型だけで、
  振る舞いは走らせて目で見るしかない。**読む前に画面を立てておくこと。**
```

### 2026-09-12 — 通し確認（起動 → 検証 → 実バックエンドでの画面動作）

```text
日付: 2026-09-12
対象ID: なし（W12 完結後の通し確認）
今回の範囲: backend 起動、verify 3本、E2E、smoke 2本、k6、
            そして**実バックエンドに繋いだ状態での画面の動作確認**。
            ずれていた README の記述の修正。
対象外: 新しい機能・新しいテスト層。W14 / W15 / W16。Stage 3。
完了条件: 上記がすべて緑で、詰まった箇所が README から読めること。

起動で詰まったこと（ユーザが踏んだエラー）:
  backend:up が `Bind for 0.0.0.0:8081 failed: port is already allocated` で失敗。
  併存リポジトリ ../learn-ClearML が起動しており 8008/8080/8081 を持っていた。
  AGENTS.local.md には書いてあるが **README には無かった**。
  あちらを down してから up し直して 15 コンテナすべて起動。

実行した確認コマンドと結果:
  pnpm run verify:python  → 緑（coverage 88%）
  pnpm run verify:web     → 緑（Angular unit 270 + report-widgets 3）
  pnpm run verify:security→ 緑（No credential was found）
  CI=1 pnpm run web:e2e   → 27/27 緑
  pnpm run serving:smoke  → SMOKE OK（401/403/422 の拒否まで含む）
  pnpm run ops:smoke      → SMOKE OK（9項目）
  pnpm run backend:data:status → 半導体学習データ登録あり
  pnpm run serving:load   → LOAD TEST OK（W12-8 を初めて実走。記録は上の節）
  エンドポイント9つへ curl → 8091 の 401 以外すべて 200
    （8091 は資格情報必須が仕様。ops:smoke が認証付きで 200 を確認している）

画面の動作確認（fixture ではなく実バックエンド）:
  使い捨ての Playwright spec で確認し、確認後に削除した。
  /projects        → Semiconductor Quality Prediction が 94 Task で出る
  /quality-pipeline→ 「Serving now」が実データを出す
                     model version 1.0.0-20260907T233649Z-eeec903c
                     promoted by <ユーザー名> / rollback の理由文まで表示
                     これは serving:smoke の `ready:` と同じ値で、
                     **画面と推論サービスが同じものを指している**ことになる
                     複製元が無い状態の案内文（pnpm ml:pipeline --submit）も出る
  4xx/5xx の API 応答とページエラーは 0 件

ここで分かったこと（README に無かった実害）:
  **4200 のログイン画面からは入れない。**
  simple mode のログインは選んだ利用者に「なりすます」形で認証する
  （login.service.ts の login() が X-Clearml-Impersonate-As を付ける）。
  なりすましは system 権限にしか許されておらず、clearml.conf に置く
  資格情報は Settings > Workspace 発行の user 権限である。
    一覧に無い名前 → auth.create_user が 403 (not allowed for role)
    一覧から選ぶ   → auth.login が 400 (Impersonation error)
  標準Web画面（8080）が同じ画面で入れるのは、ClearML Server 内蔵の
  system 権限の資格情報を使っているため（実際の Authorization ヘッダで確認）。
  入り方は「先に 8080 でログインし、そのまま 4200 を開く」。
  cookie はホスト名に対して出るのでポートでは分かれない。
  ただし **localhost と 127.0.0.1 は別ホスト**で、8080 を localhost で開いて
  127.0.0.1:4200 を開くとログイン画面へ戻される（実測）。
  これは user 権限の鍵で SDK（学習・seed・推論）が動くことと矛盾しない。
  なりすましだけができない。

自分で加えた変更（すべてドキュメント）:
  README.md
    「1. ClearML Serverを起動する」へ 8008/8080/8081 の競合と対処を追加
    「5. ログインする」を新設（上記の入り方・落ち方の表・localhost の注意）
    E2E の件数 18 → 27（W12-6 / W12-7 で増えた分が未反映だった）
    400エラーの節の `~/clearml.conf` → `./clearml.conf`
      （生成は ~/ を読まない。AGENTS.local.md とも矛盾していた）
    目次へ「ログインする」を追加
  README.初期化方法.md
    `corepack pnpm python:setup` → `py:setup`（存在しない script だった）
  x-plan-改訂/01_チェックリスト.md
    W12-8 の「未実走」を実測値に置き換え

追加したテスト:
  なし。実バックエンドでの確認は使い捨ての spec で行い削除した。
  常設にしなかったのは、backend 起動とログイン済み cookie が前提になり、
  test-pyramid の e2e 層（fixture で閉じる）の前提を変えてしまうため。

意図的に壊したテストと確認結果:
  なし（今回はコードを変えていない）。
  ただし README の修正前後で症状は再現・確認済み。
    127.0.0.1:4200 → /login へ戻る / localhost:4200 → /quality-pipeline のまま
    という差を実測して、cookie がホスト名で分かれることを確かめた。

説明できるようになったこと:
  この構成で画面を見るには **8080 で先にログインする**必要があること、
  そしてその理由が「user 権限はなりすませない」という1点であること。
  「動かない」の多くは壊れているのではなく入り方が書かれていないだけだった。

まだ説明できないこと:
  4200 単独でログインを完結させるべきかどうか。
  やるなら system 権限の資格情報を画面へ渡すことになり、ADR 003 の
  「生成物に資格情報を残さない」と正面からぶつかる。**いまは判断しない。**
  k6 の余裕が p95 で 200ms しかない件。VU を増やしてどこで超えるかは未測定。

次の一手:
  W14 / W15 / W16 のどれか、または Stage 3 の P0-1。
  P0-1 をやるなら、上の手順で先に画面を立ててから読むこと。
```
